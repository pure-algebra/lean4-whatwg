# URL reification plan

Status: U0/U1 setup and U2a inventory verified; U2b strict join and U2c
authored reader/projections verified; declaration/assurance and coverage
joins still open, 2026-09-05,
on `codex/url-reification`
from `main` at `c1c7caa`. The requested end state is the WHATWG URL Standard
reified in Lean 4 with specification-level algorithms and laws, strong proof
assurance, and idiomatic native Lean APIs. A pin or compiling scaffold alone
does not meet that objective.

User stop, 2026-09-05: URL implementation was paused to prioritize removing
`Classical.choice`; the user then requested wrapping up after the nine
integer proof repairs. `docs/CHOICE-REMOVAL.md` records the verified repair
and remaining dependencies. No core rebuild was started. Resume only on a
new user instruction; the full reification objective remains unfinished.

## Authority and ownership

`SPEC-MANIFEST.md` owns the URL source pin and section dispositions.
`docs/PROVENANCE.md` owns upstream identity, fetch commands, licenses, and
digest cross-checks. `docs/ARCHITECTURE.md` owns module boundaries. This file
owns URL phases, entry/exit gates, and the current handoff.

The setup follows Infra's W5 and HTML's H0/H1 in the existing `whatwg`
package. `Whatwg.Url` is the consumer root, imported from `Whatwg.lean` and
therefore from the common axiom audit. It adds no Lake package or dependency.
The existing pinned toolchain, warning policy, gates, and CI apply.

The URL upstream tree supplies `url.bs`, its license, and its contribution
README. It has no Streams-style reference-implementation directory. No
third-party URL implementation is silently promoted to semantic authority.

## Phases

| Slice | Deliverable | Exit evidence |
| --- | --- | --- |
| U0 | source and license pin, WPT `url/` at the existing WPT pin, provenance | every fetched file matches its upstream Git blob hash and size; manifest generated by Lean and independently cross-checked; vendor seal PASS |
| U1 | declaration-free breadth scaffold for every major URL area | `lake build Whatwg.Url` and `lake env lean Whatwg/Url.lean`; reachable from the common audit root; independent review; baseline gate limitations recorded |
| U2 | URL census, dependencies, public declaration records, assurance routes, and representative breaker contracts across all major areas | exhaustive inventory anchored by source byte spans and digests; no unused disposition input; Lean regeneration/drift and denominator/numerator join gates; each exported declaration has one canonical owner or named relationship |
| U3 | percent-encoding sets, byte decoding and encoding, scalar helpers | frozen red packet then builder; typed boundaries for non-UTF-8 encoders; general algorithm laws and malformed-input witnesses |
| U4 | host representation, IPv4, IPv6, opaque hosts, domain conversion | frozen representative contracts; parser/serializer laws with exact domains; Unicode UTS #46 pin and implementation or an explicit limited boundary claim |
| U5 | URL records, basic parser states and overrides, serializer, equivalence | total parser with progress/termination argument; each state transition anchored; base URLs, file paths, null/empty fields, validation errors and failure distinguished; canonical-domain laws |
| U6 | origins, rendering requirements, cross-standard dependencies | named HTML/File API dependencies; blob and opaque origins accounted for; requirements distinguished from deterministic serialization |
| U7 | form-urlencoded parsing, serialization, hooks | ordered tuple-list model, UTF-8 and encoding behavior, plus/space rules, round trips on their stated domains |
| U8 | URL and URLSearchParams API, mutations and association | exact constructors, getters, setters, static methods, iteration, sort/update rules; Web IDL and host identity boundaries; API laws under named observations |
| U9 | conformance evidence and integration | exact host profiles and WPT corpus/runner pins; each disagreement recorded; generated-code relations only if implemented and proved; separate URL coverage report from checked Lean data |
| U10 | final assurance audit | every intended source row classified and accounted for, no hidden open graph edge or leaf receipt; all required gates green; independent model-intent, proof-trust and spec-fidelity review |

Before developing one area deeply, U2 freezes a representative contract for
percent encoding, hosts, the URL record/parser, serialization/equivalence,
origins/rendering, form encoding, and each API class. A separate breaker
process commits contracts and a red battery before builder implementation.
Red modules use `test/fixtures/trust-gate/known-red.txt`; stable URL attack IDs
join the central counterexample register. Frozen theorem statements remain
unchanged by builders.

Native Lean decisions are made against the exact installed core/Std APIs and
existing Infra owners, with measurements when representation choices affect
kernel reduction or compiled execution. A convenient carrier, finite probe,
or automation tactic is not evidence of semantic fidelity. No `sorry`, new
axiom, `native_decide`, or `bv_decide` closes an obligation.

The user's 2026-09-05 objective additionally minimizes `Classical.choice`
in proofs as far as practical. Constructive definitions and proofs are the
default for this lane. Each semantic declaration's receipt must inspect its
actual transitive axioms, including dependencies in its statement; absence
of a written `classical` command is insufficient. Prefer the canonical
Infra byte/code-point/string carriers, explicit decidable predicates and
structural induction. Before retaining a dependency on choice, identify its
exact path and try a constructive proof or an existing constructive API.
Keep any remaining dependency and the reason it remains in that declaration's
assurance record. Tooling and host-facing text adapters have separate receipts
so their dependencies cannot be mistaken for a requirement of URL semantics.
The repository's standard-base ceiling still rejects forbidden axioms; its
permission for `Classical.choice` is not this lane's proof-quality target.

## Dependency questions to close at U2

Infra already owns byte, code-point, scalar-value, and JavaScript string
carriers. Reuse them; name any checked view or adapter and its conversion law.
Lean `String` cannot silently replace a UTF-16 string containing unpaired
surrogates. Validation errors may accompany successful parses and must not be
collapsed into failure.

URL invokes Encoding, Unicode UTS #46, Web IDL, HTML, the File API, and the
Public Suffix List for public suffix and registrable domain algorithms. Its
requirements also refer to the IANA URI Schemes registry and Unicode bidi
rendering behavior. Their
exact algorithms, tables, versions, and admitted host boundaries need explicit
rows before use. URL's pure helpers and parser have first-order inputs and
outputs; URLSearchParams association and API mutation need explicit state and
observation laws. Streams' promise tape and masks are not automatically URL's
observation model. Each URL contract must name its own observation projection
and explain any relation to the common masks.

The fetched WPT `url/` directory includes parser, setters, form, IDNA, and API
fixtures. It is corpus material only: browser infrastructure, `/resources`,
IDL interfaces, and cross-directory Encoding/HTML cases are not bundled by
this slice. WPT's own `url/README.md` locates encoding-argument tests under
`/encoding` and `/html/infrastructure/urls/resolving-urls/query-encoding/`.
Pin the required runner and these inputs before claiming any host result.

### Census scanner acceptance requirements

Independent review of the pinned URL bytes found that neither existing
`Gates.Census` profile suffices unchanged. The Streams profile requires its
piping list and only recognizes `<xmp class="idl">`, whereas URL has two
`<pre class=idl>` blocks. The Infra definition profile misses IDL and unnamed
algorithm blocks, including API setters and origin extraction. URL's bare
`id=` and `for=` attributes also require the general attribute reader.

U2 must account for all 208 definition openers, 72 algorithm blocks, 21
parser-state definitions, and two IDL blocks independently counted at this
pin. These are scanner cross-checks, not specification coverage. Definition
spans must include associated tables (special schemes/default ports), brief
unordered lists (Windows drive-letter and valid-URL grammar), and table-cell
error definitions. Parser states need their own body spans and a parent
parser relationship. The current next-blank-line and next-ordered-list rules
can omit necessary text or absorb an unrelated algorithm. Add executable
scanner contracts for those shapes before admitting the URL census.

## Verification and handoff

Base: `c1c7caa9b68ba4ff72ac379f4aedcc84385e5f28`. Breaker packet chain:
`afcd91d` (initial inventory), `4e4e880`, `6db5ec2`, `a60add5` (review
regressions). These commits precede builder implementation changes.
The builder landing includes the bootstrap and prerequisite repairs on
`codex/url-reification`; no remote branch or pull request has been published.
The file fence is recorded in `COORDINATION.md`. New files are the URL root
and eleven declaration-free area modules, this plan, three URL upstream files,
and 49 WPT URL files. Shared changes wire the root, authority/provenance,
architecture, phase and coverage scope, and the Lean-generated vendor seal.
No semantic URL declarations or URL execution theorems exist yet; semantic
proof edges are not opened or closed by these stubs. U2a's tooling declarations
and finite counterexamples have the separate receipt below.

### U2a source inventory receipt

`Gates/UrlInventory.lean` implements the frozen tooling surface in
`docs/URL-INVENTORY-INTERFACE.md`. It lexes UTF-8 bytes, checks their complete
token partition, and retains definition, algorithm, IDL, prose, table, list,
and heading candidates with exact spans. The command `lake exe urlinventory`
checks the pin, fixed-source candidate checks and the deterministic
`generated/url-source-inventory.tsv` projection; `--write` regenerates it.
The default targets, CI and `WhatwgTest.lean` import this tooling and its
frozen finite batteries.

The separate breaker committed the original eleven probe groups before
implementation and observed the missing-module red build. Independent review
then found five lexical defects: owner truncation, form-feed whitespace,
raw `xmp` content, IDL header shape and paragraph block boundaries. A separate
breaker retained ten regression assertions across `URL-INV-CE-001` through
`URL-INV-CE-005`; all ten failed against the first implementation. Repairs
leave every frozen assertion unchanged. Both batteries now pass, their
known-red entries are removed, and the central counterexample register
records the finite regression closures.

A one-time independent Python `HTMLParser` reader compared every emitted
candidate's kind, start/end bytes, label and section. Python `hashlib`
independently recomputed every span SHA-256. All 1473 candidates agree. The
scratch reader initially mishandled comments and explicit paragraph closers;
those four discrepancies were resolved against the frozen rules without
changing the Lean output. This is independent fixed-pin evidence; Python is
not a repository gate. The durable candidate ledger is the Lean-generated TSV.

Final U2a commands and receipts:

| Command | Result |
| --- | --- |
| `lake build WhatwgTest.Url.InventoryContract WhatwgTest.Url.Counterexamples.Inventory` | PASS: original eleven probe groups and all ten regression assertions |
| `lake build` | PASS, 265 jobs; common gate checks 127 modules, 6774 declarations, including 1797 tooling declarations |
| `lake exe urlinventory` | PASS; source pin, byte partition and unchanged projection |
| `lake exe vendorseal`, `lake exe citations`, `lake exe tyxmlschema`, `lake exe census` | PASS; Streams census and TyXML projections unchanged |
| `#print axioms Gates.UrlInventory.tokenize`, `.scan`, `.cli` | `[propext, Classical.choice, Quot.sound]` |
| `#print axioms Gates.UrlInventory.checkPartition` | `[propext]` |

These are lexical tooling receipts. No URL semantic declaration or URL
coverage denominator has been added. `docs/URL-CENSUS-DAG.md` records the
open classification, semantic-span, dependency, declaration and coverage-join
edges. The next packet must classify the inventory and freeze containing
semantic spans, especially parser states, shared bodies, tables and lists.
The remaining representative semantic contracts and U3–U10 work remain open.
Independent re-review confirmed all five fixes and the unchanged frozen
assertions; it found no additional actionable defect in the repairs.

### U2b assignment and authored-source receipt

Base: U2a builder `79c23294e0d4a4354f8376d9c83551635546af44`.
The separate breaker committed the assignment packet at
`6fe8b5c14db96f2d51a3004954af76947bdbc7bc` and retained the review regression
at `cb4cee5a820ad484cae3b3c8dcf3ed63c40eb87e` before its repair. The builder
landing contains `Gates/UrlCensus.lean`, its interface record, the authored
`census/url/` inputs, source-review notes, test/root integration, registry
closure and this handoff. No frozen packet or assertion is changed.

`SourceRow` adapts the existing census row and disposition carriers.
`Explanation` records a justified region and `Assignment` is a derived
inventory view. Their single owner and frozen declarations are in
`docs/URL-CENSUS-INTERFACE.md`. `assign` scans the source itself, validates
unique anchors, IDs, origins, interval parents and dependency identities,
and accounts for every candidate without an implicit exclusion. The
original battery has 67 finite assertions. Independent review discovered
form-feed-only explanations were accepted; the breaker retained two failing
refusals before the explicit five-character ASCII whitespace repair.

The authored span and explanation files now retain algorithm bodies,
parser states, IDL statements, fields and initial values, validation-error
table conditions, grammar lists, prose laws, rendering requirements and
specific explanations of examples. Independent review checked the fixed-pin
origin/interval forest and candidate assignments and confirmed the final
data corrections. These files are not yet read by a repository gate.
They do not supply dispositions, dependency pins or canonical declaration
records. No URL denominator, semantic implementation, or semantic theorem
is admitted by this checkpoint.

| Command | Result |
| --- | --- |
| `lake build WhatwgTest.Url.CensusContract WhatwgTest.Url.Counterexamples.Census` | PASS; all 67 original assertions and both regression refusals unchanged |
| `lake build` | PASS, 269 jobs; common gate checks 130 modules and 6896 declarations, including 1898 tooling declarations |
| `lake exe vendorseal`, `lake exe citations`, `lake exe tyxmlschema`, `lake exe census`, `lake exe urlinventory` | PASS; existing projections remain unchanged |
| `#print axioms Gates.UrlCensus.SourceRow`, `.Explanation`, `.Assignment` | no axioms |
| `#print axioms Gates.UrlCensus.assign` | `[propext, Classical.choice, Quot.sound]` |

The remaining graph edges are recorded in `docs/URL-CENSUS-DAG.md`; none is
closed merely by these finite probes. The next source-tooling packet must
read and validate the authored inputs, generate anchors/digests, and join
reviewed dispositions, dependency identities, declaration records and
coverage witnesses. Representative semantic contracts across all major URL
areas remain required before implementation develops one area deeply.

### U2c authored reader and source projection receipt

Base: scalar builder `9c8cb49d92393ed289fa034128a5e031b1fe0c8c`.
Separate breaker `9e545357de635b5cccc6fe76cec934ec0253318c` froze the
six-string `Inputs` view, exact `build`/`project`/`cli` signatures and 77
finite assertions in `test/contracts/url-census-input.contract.md` and
`WhatwgTest/Url/CensusInputContract.lean`. The first narrow build failed
for the missing reader module. The original fixture and every expected
projection string remain unchanged.

The builder landing adds `Gates/UrlCensusInput.lean`, `bin/UrlCensus.lean`,
the frozen input-interface record, four authored metadata TSVs and their
dependency-meaning record. It updates test/gate roots, default targets,
CI, source-review/graph/authority status, the census input README, the
counterexample register, known-red and coordination handoff. Lean writes
`generated/url-census.tsv` and `generated/url-source-assignments.tsv`.
This landing's commit is the head of this receipt, identified by Git history.
No URL semantic declaration, imported dependency pin, WPT host run or
coverage numerator is added.

The reader uses the existing census row/disposition vocabulary and resolver,
then the strict U2b assignment. It checks exact syntax, nearest-heading
dispositions with no fallback, used overrides/rules, one dependency entry
per row, external identities, in-span unique prefix anchors and all source
assignments. Both projections hash the exact source and six authored input
texts, including comments and line endings. Check mode compares every byte
in both files and reports every differing line, including invalid UTF-8.

Independent source review checked all authored dispositions and dependency
lists. Its three fixes retain Infra surrogate/noncharacter predicates,
the pointer's code-point/length/EOF relationships, and the byte decoder's
advisory UTF-8 reference. All 79 external identities are used; their
implementations, precise declaration mappings and assurance remain open.
An independent raw-byte/`hashlib` check agrees with all 282 sorted source
rows, their first unique anchors, all 1473 candidate assignments, source
fields and all raw-input hashes. These are source/tooling counts only.

Independent review also confirmed that the initial reader normalized a
terminal carriage return twice when calling the shared disposition parser.
Breaker `47426780d04fde05e566a2093663bc58749e7381` froze five probes before
repair, recording the uncommitted implementation digest. Two positive
controls elaborated and all three malformed-input refusals failed. The
reader now adds a fresh CRLF terminator for the shared parser to consume,
preserving existing field bytes. The five probes remain in the default
test root, known-red is empty, and `URL-INP-CE-001` is closed as a finite
format regression. Independent re-review confirms the repair and verifies
its exact source fence by reconstructing the frozen pre-repair digest.

| Command / check | Result |
| --- | --- |
| `lake build WhatwgTest.Url.CensusInputContract WhatwgTest.Url.Counterexamples.CensusInput` | PASS; all 77 original and five regression assertions unchanged |
| `lake exe urlcensus --write`, then `lake exe urlcensus` | PASS; both projections generated by Lean; final repair leaves them byte-identical |
| Isolated direct `urlcensus.exe` CLI invocations | 14 initial cases pass: valid check, invalid arguments, both outputs missing/drifting, invalid UTF-8 in each authored file, wrong pin, invalid syntax before write, regeneration and nested root discovery; two post-repair CRLF/malformed-CR cases also pass |
| `lake build` | PASS, 277 jobs; common audit checks 134 modules, 7008 declarations, including 1966 tooling declarations |
| `lake exe vendorseal`, `lake exe citations`, `lake exe tyxmlschema`, `lake exe census`, `lake exe urlinventory`, `lake exe urlcensus` | PASS; existing Streams/TyXML/vendor/inventory projections unchanged |
| `#print axioms Gates.UrlCensusInput.Inputs`, `.Inputs.mk` | no axioms |
| `#print axioms Gates.UrlCensusInput.build`, `.project`, `.cli` | `[propext, Classical.choice, Quot.sound]`; inherited string/scanner tooling dependencies remain explicit |

Independent final projection SHA-256 values:

- `generated/url-census.tsv`: `77d13a1890818ee8372fbdb0c487744cfa29ad6b7ffb688c26275a2ffed717cf`
- `generated/url-source-assignments.tsv`: `f9ac4575eed7ea6152f246dba531d70df48474c1842843851f333e22c77bba5f`

`URL-PG-CENSUS` retains the open canonical-owner, declaration, assurance and
numerator/report joins. The next U2 packet must freeze these relationships
and representative contracts across the major URL areas before deep
semantic implementation. Full URL reification and choice minimization
remain active; this source-tooling checkpoint closes neither objective.

### Constructive dependency baseline

In response to the added choice-minimization objective, actual axiom
inspection at U2b found no axioms on the existing Infra `Byte`,
`ByteSequence`, `CodePoint`, `CodePoint.ext`, `CodePoint.ofUnit`,
`ScalarValue`, `JsString`, `JsString.unitsOfCodePoint`,
`JsString.ofCodePoints`, or `JsString.identical`. `JsString.pairValue`,
`codePoints`, `isScalarValueString` and `convert` reach only
`[propext, Quot.sound]`. These are viable canonical proof inputs without
`Classical.choice`; no duplicate carrier is needed for that purpose.

`JsString.ofLiteral` does reach choice. A dependency-path inspection traced
it through `String.toList`, `String.Internal.toArray`, the UTF-8 decoding
proofs and `Classical.propDecidable`. The U2b tooling join has a separate path
through the inventory's IDL scanner and Lean's string-pattern search proofs.
These are actual remaining dependencies, not removed by this checkpoint.
The scalar/`Char` bridge initially had choice-bearing helper proofs.
The subsequent repair in `docs/INFRA-SCALAR-ASSURANCE.md` retains all nine
existing signatures and conversion definitions and changes only two helper
proof bodies. Independent breaker `9cf3216` first observed six transitive
choice dependencies; the unchanged battery now checks all nine declarations
against `[propext, Quot.sound]`. `ScalarValue.isValidChar` and `toChar` have
no axioms. Both general inverse theorems meet the constructive ceiling.
The default build and all repository gates pass; independent proof review
found no defect. Local identity/coverage joins, string-adapter dependencies
and the full URL reification remain open.

The initial `lake build` was run before the URL scaffold existed. It exposed
pre-existing failures in `Whatwg/Infra/Text/Order.lean` (length/bounds proof
normalization) and `WhatwgTest/Audit/SpecCoverage.lean` (the census header call
omits its newly required standard argument). Repairs use explicit natural
arithmetic proof terms, a typed predicate in the isomorphic encoder, kernel
decision for existing finite probes, and the explicit Streams census argument.
The Infra root now imports all fourteen existing area files for the common
audit. The previously configured hash revision is unresolvable (GitHub 422,
Git cannot read its tree); `lakefile.toml` now agrees with the already locked
and documented `0168306b7068b97758e3f2d4307eeb97aa31a104` pin. No dependency
upgrade or lockfile regeneration was needed.

One existing finite example was false: split-on-commas does not append an
extra empty token after a final comma. The algorithm already follows the
pinned Infra text; its unfrozen example was corrected. Separate breaker
packet `INFRA-TEXT-CE-001` retains the old expectation as a refuted statement
and checks the three-token and empty-input results. This is an Infra baseline
repair, not a URL proof or a general comma-splitting theorem.

| Command / check | Result |
| --- | --- |
| Upstream Git tree versus `git hash-object --no-filters` and sizes | all 52 fetched URL source/WPT files agree; independent reviewer repeated the check |
| `lake exe vendorseal --write`, then `Get-FileHash -Algorithm SHA256` for all new rows | Lean regenerated the manifest; all 52 digests and sizes agree independently |
| `lake build Whatwg.Url`; `lake env lean Whatwg/Url.lean` | PASS, all eleven area stubs imported, no declarations |
| `lake build Whatwg.Infra.Text.Order`; `lake build Whatwg.Infra.Text.Codec`; `lake build Whatwg.Infra.Text.Whitespace`; `lake build Whatwg.Infra.Text.Scan` | PASS after the prerequisite repairs |
| `lake build` | PASS, including the common module-closure and axiom gate; final receipt below |
| `lake build WhatwgTest.Infra.Counterexamples.CommaSplit` | PASS; all three exact ascriptions elaborate, and each axiom receipt is `[propext, Classical.choice, Quot.sound]` |
| `lake exe vendorseal` | PASS, 258 files in six trees; all Windows paths valid |
| `lake exe citations` | PASS |
| `lake exe tyxmlschema` | PASS, schema projection and generated Lean modules byte-identical |
| `lake exe census` | PASS for Streams; its existing projections unchanged |

Independent bootstrap review checked source identity, area coverage, import
reachability, dependency boundaries, and proof-repair scope. The required
URL census scanner work is recorded above. URL coverage has no denominator
or report yet; none is inferred from file counts. U2–U10 remain open, and
the full reification goal remains active.

Bootstrap common audit receipt (the later U2a receipt is above):

```text
Whatwg module and axiom gate: checked 124 modules and 6529 declarations (1595 in the Gates tooling tree); semantic/test ceiling is [propext,
 Quot.sound,
 Classical.choice]; implementation ceiling (2 audit module(s), 0 exact declaration(s), plus the Gates tree) additionally allows Classical.choice
```

### U3 packet: the percent-encoding freeze (breaker, 2026-09-07)

The URL lane resumed on the operator's instruction and the U3 breaker seat froze
the first **semantic** URL packet on branch `url/u3-breaker` from `main` at
`7345c96`, in parallel with the promise lane. It shares no file with that lane
beyond `WhatwgTest.lean` and `test/fixtures/trust-gate/known-red.txt`, both
append-only for it. Everything before this point in this document is source
tooling; this is the first packet that states a URL algorithm.

Packet: `test/contracts/url-percent-encoding.contract.md`.
Graph: `docs/URL-PERCENT-ENCODING-DAG.md` (`URL-PG-PERCENT`).
Attacks: `test/counterexamples/url/PERCENT-ENCODING.md`, `URL-PE-CE-001`
through `URL-PE-CE-014`, all `SEEDED`.

**Scope.** Twenty census rows of `generated/url-census.tsv`, the contiguous byte
range [16209,25393) of the pin: 15 `op`, 3 `rule`, 1 `type`, 1 `requirement`.
The breaker recomputed all twenty span digests and all twenty anchor digests
from the sealed bytes with .NET SHA-256; every one agrees with the census, and
no vendored or generated byte changed. Seventeen further rows depend on a scoped
row and are named in the contract as consumers only — the host and opaque-host
parsers, five parser states, the two credential setters, `op.url-units`, three
URL-writing rules, the domain validation rule, the urlencoded parser and
serializer, the search-params encoding rule, and the rendering requirement.
Twenty plus seventeen is the 37 `percent`-anchored rows `COORDINATION.md`
records for the seat.

**Design.** A percent-encode set is a decidable predicate on the canonical
`Whatwg.Infra.CodePoint`, and the eight named sets are built upward from the
C0 control set by one `extend` construction following the text's own
"consisting of" sentences, so that the seven inclusion edges the census records
are theorems and the two owned rules — the query/fragment incomparability and
the form complement — are theorems as well. A separate finite `SetName` index
carries the identity comparisons steps 1 and 2 of
`op.percent-encode-after-encoding` perform, since a bare predicate has no
decidable equality; `setOf_eq` pins it and `setOf_injective` separates it.
Percent-encoded byte syntax, percent-encode and percent-decode are stated over
Infra's byte, byte-sequence and string carriers with no new carrier of any kind.

**The Encoding boundary, and a ratification request.** Five operations the
scoped rows call — `UTF-8 encode`, `UTF-8 decode without BOM or fail`,
`get an encoder`, `encode or fail`, the I/O queue — have no owner in this
repository, and Infra explicitly defers to Encoding for all of them. DB-02
applies: the packet makes each answer first-order data
(`Whatwg.Url.Boundary.EncoderTape`, `Utf8Encoding`, `Utf8DecodeOrFail`) and
keeps every URL-written step total and owned, including the two UTF-8
specializations. The ISO-2022-JP encoder is the named stateful case, carried as
`EncoderName.isStateful` and witnessed by `URL-PE-CE-006`; it is also the reason
`op.parser-query` buffers, which U5 will need.

The breaker **recommends pinning the Encoding Standard now**, as a source, the
way Web IDL and ECMA-262 were pinned beneath Streams by DB-11, with its UTF-8
core landing in `Whatwg.Infra.Text.Codec` beside `isomorphicDecode` and
`asciiEncode`. Three of the twenty rows are otherwise unclosable, and the same
boundary is reopened by U4, U6 and U7. Pinning is outside the breaker's fence,
so the ruling is flagged here and in the contract; the alternative is to defer
to U7 or U9 with the boundary named exactly as frozen, in which case those three
rows stay `partial` and no `utf8Encode` is invented in the meantime. The packet
is buildable and closable under either ruling.

**Batteries.** 60 interface ascriptions in
`WhatwgTest/Url/PercentEncodingContract.lean`, 95 theorem ascriptions in
`WhatwgTest/Url/PercentEncodingLaws.lean` (82 general laws and 13 finite probes
transcribed from the source's own example table at [25459,27305)), the same 95
receipts in `WhatwgTest/Url/PercentEncodingAxiomReport.lean`, and 15 finite
mutant witnesses in `WhatwgTest/Url/Counterexamples/PercentEncoding.lean`. The
expected declaration delta is 155 named public declarations across the two
today-declaration-free modules `Whatwg/Url/PercentEncoding.lean` and
`Whatwg/Url/Boundary.lean`.

**Mask.** None. This family is equational, a Stratum A atom over Stratum V data
(RS-2), and `docs/SPEC-COVERAGE.md`'s green criterion covers that case
explicitly. DB-04's M1 and M2 are Streams' consumer observations and are not
retrofitted here. The contract records the decision once, for all twenty rows.

| Command | Result |
| --- | --- |
| `Get-FileHash -Algorithm SHA256` on the pinned `url.bs` | `a5aa827f…3c805`, 162,680 bytes; agrees with the census header |
| Independent .NET SHA-256 over the twenty spans and their anchors | all forty digests agree with `generated/url-census.tsv` |
| `lake --wfail build Whatwg Gates` | PASS, 164 jobs |
| `lake env lean -DmaxErrors=5000` on the three ascription batteries | exit 1; 114 + 354 unknown identifiers and 95 unknown constants, 563 diagnostics over 155 distinct names, all under `Whatwg.Url.PercentEncoding` or `Whatwg.Url.Boundary` |
| `lake env lean -DmaxErrors=5000` on the counterexample battery | exit 0; fifteen mutant witnesses pass by kernel reduction |
| `lake build WhatwgTest` | exit 1; exactly the three declared known-red modules log failures |

No parse error, import error, type mismatch, or failed instance synthesis
appears in the red output. No graph edge of `URL-PG-PERCENT` closes at this
freeze, no URL coverage denominator, numerator or report exists or is implied,
and U3 through U10 remain open.

### U3 landing receipt (builder, 2026-09-07)

Branch `url/u3-builder` from `url/u3-breaker` at `5fa88a2`, merged with `main`
at `105d218` (one documentation commit, `COORDINATION.md`, no conflict). The
builder implemented the two modules the packet fences and nothing else.

**Files changed by this seat.** `Whatwg/Url/Boundary.lean` and
`Whatwg/Url/PercentEncoding.lean` (both declaration-free stubs before),
`Whatwg/Url.lean` (root docstring only, from "declaration-free bootstrap stubs"
to the landed surface), `test/fixtures/trust-gate/known-red.txt` (the three U3
entries removed, the packet note kept and extended),
`docs/URL-PERCENT-ENCODING-DAG.md` (landing rows) and this subsection. **No file
under `WhatwgTest/` changed at all**: `git diff --name-only 5fa88a2 --
WhatwgTest` is empty, so the three frozen batteries and the fifteen retained
finite witnesses are byte-identical to the freeze and no elaboration repair was
needed. Nothing under `vendor/`, `generated/`, `census/`, `Gates/`,
`WhatwgTest/Audit`, `Whatwg/Infra`, `Whatwg/Streams`, `SPEC-MANIFEST.md`,
`PLAN.md`, `COORDINATION.md` or `test/counterexamples/REGISTER.md` changed.

**Statement fidelity.** All 60 interface ascriptions and all 95 law ascriptions
elaborate with no token changed, and the 95 `#print axioms` names all resolve.
Acceptance condition 1 is met with an empty repair list.

| Command | Result |
| --- | --- |
| `git merge --no-edit origin/main` | clean, one commit (`COORDINATION.md`), no conflict |
| `lake --wfail build Whatwg Gates` | PASS, 164 jobs |
| `lake env lean -DmaxErrors=5000 WhatwgTest/Url/PercentEncodingContract.lean` | exit 0, 0 diagnostics (was exit 1, 114) |
| `lake env lean -DmaxErrors=5000 WhatwgTest/Url/PercentEncodingLaws.lean` | exit 0, 0 diagnostics (was exit 1, 354) |
| `lake env lean -DmaxErrors=5000 WhatwgTest/Url/PercentEncodingAxiomReport.lean` | exit 0, 0 diagnostics (was exit 1, 95) |
| `lake env lean -DmaxErrors=5000 WhatwgTest/Url/Counterexamples/PercentEncoding.lean` | exit 0, unchanged; all fifteen mutant witnesses still pass by kernel reduction |
| `lake --wfail build WhatwgTest` | PASS, 229 jobs |
| `lake exe trustselftest` | PASS; "declared red set matches the observed red set (0 module(s))", every planted `partial`, `unsafe`, `sorry`, `native_decide`, malformed-literal and unreachable-module probe rejected for its stated reason |
| `lake exe vendorseal` | PASS, manifest and `vendor/` agree in both directions; every path valid on Windows |
| `lake exe citations` | PASS, 344 files scanned, no line-numbered citation into a protected authored document |
| `lake exe urlinventory` | PASS, pinned bytes, 1473 source candidates, projection byte-identical |
| `lake exe urlcensus` | PASS, both projections byte-identical |
| `lake exe census` | PASS (streams), 450 rows, denominator 410, 12 green / 6 partial / 392 absent, both projections byte-identical |
| `lake exe census --standard infra` | PASS, 176 rows, denominator 165; no numerator exists for this standard |
| `lake exe census --standard webidl` | PASS, 124 rows, denominator 116, emit agrees row for row |
| `lake exe census --standard ecma262` | PASS, 77 rows, denominator 75, emit agrees row for row |

**Measured audit delta.** The gate line is
`Whatwg module and axiom gate: checked N modules and M declarations (1477 in the
Gates tooling tree)`. Before, measured on this branch with the three red battery
files and their three imports temporarily removed so the build could complete:
**197 modules, 13055 declarations**. After: **200 modules, 13584 declarations**.
The three modules are the restored batteries, which declare nothing. The
**+529 declarations** are all in the two implemented modules and decompose as
**155 authored public names — exactly the contract's expected 155 — plus 135
private helpers and 239 Lean-generated constants**. The generated constants are
constructors, field projections, recursors, `noConfusion`/`noConfusionType`,
`below`/`brecOn`/`binductionOn`, the derived `DecidableEq` and `Repr` instances,
`.elim`/`ctorElim`/`ctorElimType`/`ofNat_ctorIdx`, equation lemmas and `eq_def`,
`match_*` and `proof_*` auxiliaries, `percentDecodeBytes.induct`, and two
`congr_simp` lemmas Lean attributes to this module for
`Whatwg.Infra.CodePoint.mk` and `Whatwg.Infra.JsString.pairValue`.

**The 95 receipts, by ceiling.** All inside R-11 (`propext`, `Quot.sound`,
`Classical.choice`), and **none reaches `Classical.choice`**:

| Axioms | Receipts |
| --- | --- |
| (none) | 42 |
| `[propext]` | 29 |
| `[propext, Quot.sound]` | 24 |
| `[propext, Classical.choice, Quot.sound]` | 0 |

**Graph edges.** Four of the ten `URL-PG-PERCENT` edges close at this landing —
`construction`, `semantics`, `laws`, `trust` — with the theorem names recorded
in the "Landing" section of `docs/URL-PERCENT-ENCODING-DAG.md`. Five stay
`required-open`: `identity` (no URL declaration-snapshot join exists),
`representation` (same, plus the unruled Encoding owner), `counterexamples` (the
production half is discharged, the central `REGISTER.md` rows are the
coordinator's), `bridges` (both bridges unproved) and `coverage` (URL has no
denominator, numerator or report; none is inferred from a green battery).
`targets` stays `not-applicable`. **The encoding obligation stays open**: no
ruling on pinning the Encoding Standard had arrived when this landed, so option
(B) holds, `op.percent-decode-string`, `op.utf8-percent-encode-code-point` and
`op.utf8-percent-encode-string` keep their unowned UTF-8 half, and no
`utf8Encode` is declared anywhere.

#### Builder notes

No ascription failed to elaborate, so no frozen statement carries a diagnostic
and the contract's statement table is untouched. The notes below are the
implementation facts a reviewer needs and the Infra candidates the packet
produced; the same list is appended to the contract as an additive
"Builder notes" section that changes no frozen statement.

- **B1 (Infra candidate).** `JsString.codePoints` has no lemma saying that a
  string with no leading surrogate has one code point per code unit. Proved
  locally as the private `codePoints_of_no_lead :
  (∀ u ∈ s, u.toNat < 0xD800) → JsString.codePoints s = s.map CodePoint.ofUnit`.
  Its natural home is beside `codePoints` in `Whatwg/Infra/Text/String.lean`.
  Nothing under `Whatwg/Infra/` was changed.
- **B2 (Infra candidate).** `JsString.isAsciiString` has no introduction rule
  from a per-code-unit bound. Proved locally as the private
  `isAsciiString_of_units : (∀ u ∈ s, u.toNat ≤ 0x7F) → s.isAsciiString = true`.
  Home: `Whatwg/Infra/Text/String.lean`.
- **B3 (Infra candidate).** `JsString.isomorphicEncode` is written with
  `List.pmap` and is therefore opaque to `rw`. Proved locally as the private
  `isomorphicEncode_eq : isomorphicEncode s h = (codePoints s).map (fun c =>
  UInt8.ofNat c.val)`, through a generic private `pmap_eq_map_of`. Home:
  `Whatwg/Infra/Text/Codec.lean`.
- **B4 (Infra candidate, the load-bearing one).** All four round-trip laws are
  stated through `JsString.asciiEncode?`, which is a `dite` over
  `isAsciiString` wrapping the `pmap`; without a computed form the laws cannot
  be discharged at all. Proved locally as the private `asciiEncode?_eq :
  (∀ u ∈ s, u.toNat ≤ 0x7F) → asciiEncode? s = some (s.map (fun u => UInt8.ofNat
  u.toNat))`. Home: `Whatwg/Infra/Text/Codec.lean`, beside `asciiEncode?`.
- **B5.** `(UInt16.ofNat n).toNat = n % 65536` is `rfl` but core states its
  lemma only for the `OfNat` literal form, so the private `toNat_ofNat16` was
  added. This is a Lean-core matter, not an Infra candidate.
- **B6 (choice minimization, the URL lane objective).** `omega` reaches
  `Classical.choice` exactly when it must case-split a disjunctive hypothesis
  against a goal that mixes `∨` with `∧`, or negate a conjunction in a
  hypothesis. Measured on the pinned toolchain: `(a = 5 ∨ a = 6) ↔ (5 ≤ a ∧ a ≤
  6)` by `omega` gives `[propext, Classical.choice, Quot.sound]`, and so does
  `(a = 5 ∨ a = 6) → (5 ≤ a ∧ a ≤ 6)`, while `(5 ≤ a ∧ a ≤ 6) → (a = 5 ∨ a = 6)`
  and every purely inequational goal give `[propext, Quot.sound]`. Ten of the 95
  receipts reached `Classical.choice` in the first pass —
  `userinfo_mem_iff`, `component_mem_iff`, `form_mem_iff`, `form_complement`,
  `component_complement`, `setOf_includes_non_ascii`,
  `followsUtf8Advice_component`, `utf8PercentEncodeString_isAsciiString`,
  `roundTrip_component`, `roundTrip_form` — and all ten were re-proved
  constructively, with no statement touched:
  - the three inclusive-range membership laws go through `or_congr` and a
    private `three_or_iff_range`, whose only `omega` calls are plain
    inequalities and one purely disjunctive goal;
  - `setOf_includes_non_ascii` replaces `omega` on a negated conjunction with
    `Nat.lt_or_ge` and `of_decide_eq_false`, which removed choice from its four
    dependents as well;
  - `form_complement` and `component_complement` split at U+007F: the 127 values
    below are settled by kernel reduction through `Nat.decidableBallLT` over a
    private `ofVal`, and above U+007E the C0 control set's second clause settles
    both sides.
  Final measurement: 0 of 95 reach `Classical.choice`.
- **B7.** `percentDecodeBytes` is written with four top-level list patterns
  (`[]`, `[b]`, `[b, o]`, `b :: f :: s :: t`). The more literal shape, an inner
  `match rest with`, is rejected by Lean's structural-recursion checker
  ("failed to eliminate recursive application `percentDecodeBytes rest`") and
  falls back to well-founded recursion, which would also stop the finite probes
  from reducing in the kernel. The two short patterns are step 2.2 with fewer
  than two following bytes, which the docstring records. For the same reduction
  reason `decimalDigitsAux` takes an explicit `Nat` fuel — the value it is
  printing, a size the data carries — rather than recursing well-foundedly on
  `value / 10`.
- **B8.** `decide` refuses a goal containing free variables. The four
  `percentEncodeAfterEncoding_example_*` probes quantify over an unused encoder
  label, so each is preceded by `rw [percentEncodeAfterEncoding_eq]`, which
  discharges the ignored encoder argument before the kernel reduces the rest. No
  statement changed.
- **B9.** `percentEncodeAfterEncoding` does not read its `encoding` argument —
  step 1 is the separate `encodingAssertion` predicate, exactly as the frozen
  interface splits them — and `utf8PercentEncodeCodePoint` does not read its
  `scalarValue`. Both binders are spelled with a leading underscore, as is the
  unused hypothesis of `utf8PercentEncodeCodePoint_eq`, because
  `linter.unusedVariables` is an error under the package's
  `warningAsError = true`. This is a binder-name choice; the frozen signatures
  and propositions are unchanged.
- **B10.** `setOf_injective` is proved from eight separating code points —
  U+0020, U+0023, U+0027, U+003F, U+002F, U+0024, U+0021 and U+0060, which are
  the seven strictness witnesses plus the omission
  `rule.query-fragment-encode-set-difference` names — collected in a private
  `separators`/`signature` pair so the 64 cases close by kernel reduction rather
  than 56 hand-written witnesses.

Nothing in this landing is reported as coverage; acceptance condition 7 holds.
U4 through U10 remain open, and the URL lane's full reification and
choice-minimization objectives remain active.
