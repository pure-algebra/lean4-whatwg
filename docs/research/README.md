# Research record

Authored research deliverables, one file per pass, each self-contained with
its commands, log paths, and pins. This index carries the findings that
later decisions cite, so that nothing depends on a session transcript.

## R0 (released 2026-09-01)

| Document | State | Findings that decisions rest on |
| --- | --- | --- |
| `2026-09-01-lean4-nlp-learnings.md` | landed | lean4-nlp at `2820c11b` shares this toolchain and zero-dependency posture and records no numbers of its own; every figure was re-measured. Lean 4.33.1's `String.Pos` API (`next`, `get`, `extract`, `Substring.toString`, also `String.length` and `splitOn`) depends on `Classical.choice`; `ByteArray.extract`, `ByteArray.get!`, `String.toUTF8`, `String.ofList`, `List.toByteArray` are axiom-free (reproduced by the coordinator with `#print axioms`). Fuel-bounded scanning with fuel = size + 1 replaces `partial` and gives an ordering theorem by induction on fuel. Index accumulation into `Array (Array _)` is up to 868x slower than packed counting sort at one bucket and slower than it when buckets outnumber entries; crossover near four entries per bucket. The tokenizer measured 7.24 MiB/s single-threaded and 3.2–3.8x on eight workers. |
| `2026-09-01-lean-stdlib-strategy-and-performance.md` | landed | Measured in a throwaway package on this host: 21 text cases on the real `index.bs`, 14 byte-carrier cases, 18 loop cases at 10^7, 7 map cases at 10^5 keys, 12 IO cases, 14 kernel files, two `#print axioms` sweeps. `BitVec 32` costs 738x `UInt32` at runtime (6572.78 ms vs 8.90 ms), refuting the SHA256-DAG section 2 prediction; in the kernel the ranking inverts (64-round `BitVec 32` loop about 3 ms, `UInt32` form about 35 ms). Under default `maxRecDepth` every plain `decide`/`rfl` known-answer test failed; only `decide +kernel` passed. Essentially the whole `String` and `String.Slice` API reaches `Classical.choice` (including `String.toSlice`, which taints the slice family); `ByteArray` is almost entirely axiom-free; `Std.HashMap` is inside the ceiling, `Std.TreeMap` and `Lean.Json.parse` are not. A 1 KiB `String` equality costs seconds in the kernel and `toList.length` did not finish in 180 s. `<div algorithm>` occurs 229 times in `index.bs` and `<div algorithm` 248 times (verified by the coordinator), so 19 blocks carry attributes and a census keyed on the closing bracket would silently drop them. `for i in [0:n]` is unfoldable via the `@[simp]` lemma at `Init/Data/Range/Lemmas.lean:88`, which lives in a different module from the notation. Left out: WPT JSON parsing cost, `Vector UInt8 n` as a document carrier, memory footprints. |
| `2026-09-01-web-reification-targets-survey.md` | landed | 40 candidates (24 WHATWG/W3C/TC39, 16 IETF) scored on eight weighted criteria; totals recomputed mechanically. Top five: WHATWG URL (4.53; named-state parser, 228,685-byte `urltestdata.json`, no verified URL parser exists in any prover), WHATWG Encoding UTF-8 core (4.49; no functional-correctness UTF-8 codec in any ITP), Streams with the promise job queue (4.25 / 4.05; the combinator prize, no prior art on backpressure), Base64 RFC 4648 (4.10), Structured Fields RFC 9651 (3.63; the one IETF spec whose algorithms bind). Infra (4.02) is the unavoidable substrate with no independent deliverable. Recommended order: Infra, Encoding, URL, Base64, Streams plus jobs, Structured Fields, Fetch last as integrator. Lean 4 has essentially no formalization of any web standard; the only presence is two WebAssembly models with zero theorems. Citation corrections: WinterCG is now Ecma TC55 and its API list is ECMA-429 1st edition (December 2025), WebSocket not included; RFC 6265bis has no RFC number and cannot be pinned; Effect v4's HTTP stack lives under `effect/unstable/http` (fetched claim, not locally verifiable from the vendored slice). Unverified items are listed in its section 7. |

## H3 (2026-09-03)

| Document | State | Findings that decisions rest on |
| --- | --- | --- |
| `2026-09-03-html-proof-and-library-patterns.md` | landed | Read-only Opus pass over the toolchain source and a synthetic 115-tag / 69-set model (no `lake` run; numbers are representative, not the repository's). `decide +kernel` cuts a 115-element reflection from ~900 to ~20 heartbeats, mints no axiom, and caches one auxiliary lemma per proposition; a `Fintype`-style `Decidable (∀ t, p t)` instance is a 2x regression. Constructor dispatch is already O(1) for membership and a bitmask cannot beat it; a `Nat` bitmask projection helps only subset lemmas (one `Nat.land`, kernel-accelerated up to thousands of bits). `deriving DecidableEq` on an enum is one `Nat` comparison on `ctorIdx` and yields `Tag.ofNat`/`Tag.ofNat_ctorIdx` for free. Nested inductives indexed by a bound content set are rejected by the kernel, so children erase to `RawNode` with evidence in `Child`; auto-params must come after every argument that fixes their index. A working `html!` elaborator skeleton carries the context as an `Expr`, evaluates with `Meta.whnf`, and reports at the offending token. For the serializer: work at `List Char` and lift with `String.ofList`; escaping soundness is `<`, `>`, `"` never survive plus `unescape ∘ escape = id` (which gives injectivity); the "no bare `&`" statement is false. Applied the same day: `decide +kernel` in Content/{Lattice,Transparent}, `Child.weaken`, the axiom-gate docstring. |

## Promise lane (2026-09-06)

| Document | State | Findings that decisions rest on |
| --- | --- | --- |
| `2026-09-06-webidl-promise-census-survey.md` | landed | Read-only pass over `vendor/whatwg-webidl-a652053f/index.bs` (695,845 bytes, digest recomputed and matching the pin); no `lake` run. Five sections in scope hold 54,343 bytes, 7.81 % of the file, 89 `<dfn>` and 26 algorithm blocks. Neither existing census profile works: the Streams profile's `scanRequirements` opens with a literal search for `<div algorithm="ReadableStreamPipeTo">` and aborts on any other source; `scanIdl` looks for `<xmp class="idl">`, which occurs zero times here (the source writes `<pre class="idl">` 11 times and `<pre class=idl>` once), and once the opener is fixed the `QuotaExceededError` block aborts on interface inheritance while the `DOMException` block's trailing `//` comment silently swallows 30 statements and yields 0 rows; the Infra definition profile aborts on four duplicate ids (`DOMException`'s associated `name`/`message` against their attribute getters, and the same pair on `QuotaExceededError`) and its paragraph fallback misplaces 274 of 382 spans, median 9,671 bytes early and worst case 115,650. Keying algorithm blocks on a `<div>` carrying an `algorithm` attribute anywhere in the tag instead of on the byte prefix `<div algorithm` is byte-neutral for both existing censuses (Streams 248 blocks, Infra 18, under either rule) and recovers the 45 Web IDL blocks that write `id=` first. `scanHeadings` admits levels 2–4, so the eleven real promise operations under an `<h5>` and the seven worked examples under another share one disposition; both existing sources have zero `<h5>` at line start, so raising the ceiling is byte-neutral. `scanSlots` would emit 47 rows, none anchored at a definition. Three definitions carry no `<dfn>` tag at all, being heading-borne (`Promise`, "exception objects", `DOMException`). Expected scoped census: 118 rows. Streams invokes eleven Web IDL promise operations 197 times — `a promise resolved with` 46, `a promise rejected with` 46, `resolve` 35, `reject` 23, `a new promise` 18, `upon rejection` 11, `upon fulfillment` 9, `react` 8, `get a promise to wait for all` 1, `wait for all` 0, `mark as handled` 0 — and the eleven reduce to five ECMA-262 operations (`Call`, `Construct`, `CreateBuiltinFunction`, `NewPromiseCapability`, `PerformPromiseThen`) plus three intrinsics. Bikeshed autolinks wrap across lines, so a single-line count returns 1,468 of the 2,188 autolinks and undercounts the two `a promise …with` operations by 12. |
| `2026-09-06-ecma262-promise-census-survey.md` | landed | Read-only pass over `vendor/ecma262-0248456c/spec.html` (2,978,793 bytes, digest re-checked against the pin, LF only, 2,189 `<emu-clause>`); no `lake` run. Two subtrees in scope, `sec-jobs` and `sec-promise-objects`: 49 clauses, 72,286 bytes, 2.43 % of the file. `aoid` occurs exactly once in scope, so a census keyed on it finds 1 of 22 operations; the operation name, parameter list and return type live only in the structured `<h1>`, and `type` takes exactly four values (`abstract operation` 15, `built-in function` 13, `host-defined abstract operation` 6, absent 15). `<emu-alg>` is not HTML: 32 blocks, 390 steps, every non-blank line matching `^ *1\. `, nesting expressed only by two spaces relative to the block's own minimum indent, maximum depth four, zero continuation lines and zero step ids. Attribute order is not fixed — two clauses write `oldids` before `id` — and ids leave the `[a-z0-9-]` alphabet: fifteen contain `.`, two contain `%`, one contains an upper-case letter, and the existing `kebab` collides `sec-promise.resolve` with `sec-promise-resolve`. Zero entity references and 188 non-ASCII bytes in four code points occur in scope, none of them structural, and there are zero tabs. Four `<emu-table>` blocks carry 8 record fields and 5 promise instance slots; nine normative `<li>` state host requirements and none of the nine carries an id; nine `<dfn>` occur and six have no id. Proposed census: 77 rows, 3 `evidenceOnly`, denominator 74, of which 44 `owned`, 13 `foreignBoundary`, 10 `hostOnly`, 7 `requirement`. Every proposed row is anchorable inside its own span, but `field.job-callback-record.host-defined` (629941–630193) is unique only at its full 252 bytes and is the first row in the repository to need the fallback past the anchor ladder. The sentence DB-03 rests on is the `HostEnqueuePromiseJob` bullet at 634739–634841, a requirement on hosts whose candidate realizer is the deterministic FIFO queue. |

## Decisions already taken on this evidence

- The P1 census generator carries `ByteArray` and `Nat` offsets, never
  `String` positions, so its definitions stay under the semantic ceiling.
- SHA-256 `Hex` (lane S1, ruling R-3) is a `List Char` producer wrapped by
  `String.ofList`, with length theorems stated on the list form; the
  exact-admission list is expected to be empty.
- Benchmarks in this repository copy lean4-nlp's calibrated-median
  methodology and keep a retired implementation as a differential oracle
  before any timing is reported.
- The seven coordinator rulings R-P1 through R-P7 (2026-09-06) rest on the two
  promise surveys: the census generator's `definitionKeyed` flag becomes a
  source profile with an ecmarkup scanner beside the Bikeshed one, ECMA-262
  gains seven row kinds and an injective id escaping, Web IDL emits no `slot`
  rows and gains an optional `rules.tsv` end locator, and the promise instance
  slots are `owned` in the ECMA-262 census while the Streams census keeps them
  `foreignBoundary`. `SPEC-MANIFEST.md` transcribes them and
  `docs/PROMISE-PACKAGE-PLAN.md` carries them out.

## How to add a pass

One file per document, named by date and subject. Every number carries its
command and log path; every external fact carries a URL and fetch date;
every claim about another repository carries its commit. No line-numbered
citation into the protected authored documents (the citation gate rejects
it). Update the table above in the same change.
