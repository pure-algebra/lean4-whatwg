/-!
# URL origins

Reserved for `origin` in the pinned URL source. HTML origin values and the File API blob URL
entry require named dependency or boundary records before implementation. Declaration-free;
see `docs/URL-PACKAGE-PLAN.md`.
-/
