/-!
# URL hosts

Reserved for `hosts-(domains-and-ip-addresses)` in the pinned URL source: host representation,
IPv4 and IPv6 parsing and serialization, opaque hosts, domain processing, and host equivalence.
IDNA requires its own pinned dependency or explicit boundary. Declaration-free;
see `docs/URL-PACKAGE-PLAN.md`.
-/
