/-!
# URL percent encoding

Reserved for `percent-encoded-bytes` in the pinned URL source: byte percent decoding,
percent-encode sets, and percent-encode after encoding. Encoding dependencies must be named
before implementation. Declaration-free; see `docs/URL-PACKAGE-PLAN.md`.
-/
