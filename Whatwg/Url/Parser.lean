/-!
# URL parsing

Reserved for `parsers` and `url-parsing` in the pinned URL source: the basic URL parser,
its states, pointer, base URL, optional existing record, and state override. Validation errors
and parser failure must remain distinct. Declaration-free; see `docs/URL-PACKAGE-PLAN.md`.
-/
