/-!
# application/x-www-form-urlencoded

Reserved for `urlencoded-parsing`, `urlencoded-serializing`, and `urlencoded-hooks` in the
pinned URL source. The ordered name/value list and space/plus behavior are separate from
general URL parsing. Declaration-free; see `docs/URL-PACKAGE-PLAN.md`.
-/
