# Whatwg counterexample register

Stable IDs in this file are never reused. A row closes only when its witness
is retained and the repaired declaration or theorem mechanically rejects the
attack. Statuses are defined in `README.md` beside this file.

| ID | Status | Attacked statement | Witness / evidence | Forced repair |
| --- | --- | --- | --- | --- |
| `WS-INFRA-CE-001` | `CLOSED` | Splitting `" a , ,b,"` on commas appends a trailing empty token | `WhatwgTest/Streams/Counterexamples/Infra/Split.lean`, `ce001_extra_trailing_token_refuted` and `ce001_comma_result`, with comma-pair, strict, whitespace, and empty-input controls; command `lake build WhatwgTest.Streams.Counterexamples.Infra.Split`; finite equational probes, no mask or host assumption | Closed 2026-09-05: corrected the false source example and draft plan; retained the kernel-checked negation and controls. The implementation's end-of-input check is unchanged. |
| `INFRA-INTEGER-CE-001` | `CLOSED` | All nine fixed-width integer range proofs must avoid `Classical.choice` without changing their carriers or statements | `WhatwgTest/Infra/IntegerConstructiveContract.lean`, frozen at `d755ba6`; packet `test/contracts/infra-integer-constructive.contract.md`; all aliases/signatures pass and all nine initial proofs fail the actual constructive axiom check | explicit native numeric bounds and signed-case conjunction proofs; five unsigned receipts have no axioms, four signed receipts use `[propext, Quot.sound]`; narrow/full builds and all gates pass; local record `docs/INFRA-INTEGER-CONSTRUCTIVE.md` |
| `URL-INP-CE-001` | `CLOSED` | U2c removes exactly one terminal CR from each authored line and preserves other field bytes | `WhatwgTest/Url/Counterexamples/CensusInput.lean`, frozen at `4742678`; packet `test/counterexamples/url/CENSUS-INPUT.md`; two positive controls elaborate while three retained-CR refusals fail before repair | add a fresh CRLF terminator at the shared-parser handoff so it consumes only that terminator; all five probes and the original 77 assertions pass unchanged; CLI CRLF and malformed-CR write checks pass; actual axiom receipt and full build/gates in the URL plan |
| `INFRA-SCALAR-CE-001` | `CLOSED` | Scalar/Char validity and inverse declarations must avoid `Classical.choice` under the stronger local proof-quality target | `WhatwgTest/Infra/ScalarConstructiveContract.lean`, frozen at `9cf3216`; packet `test/contracts/infra-scalar-constructive.contract.md`; all signatures elaborate and six declarations fail the actual transitive axiom check before repair | only two helper proof bodies replaced with explicit decidable range reasoning; all nine exact signatures and constructive receipts pass unchanged; full build, repository gates and independent proof review pass; actual receipts in `docs/INFRA-SCALAR-ASSURANCE.md` |
| `URL-CEN-CE-001` | `CLOSED` | A census explanation reason must contain a character outside the five ASCII whitespace characters | `WhatwgTest/Url/Counterexamples/Census.lean`, frozen at `cb4cee5`; packet `test/counterexamples/url/CENSUS.md`; FF-only and mixed whitespace-only reasons both failed against the initial join | explicit five-character ASCII whitespace predicate; both frozen refusals and the original census battery pass; default build, actual axiom receipt and repository gates pass; independent review confirms the repair |
| `URL-INV-CE-001` | `CLOSED` | URL inventory preserves the full normalized `for` owner | `WhatwgTest/Url/Counterexamples/Inventory.lean`; packet `test/counterexamples/url/INVENTORY.md`; finite tooling output observation | removed uncontracted first-comma truncation from `definitionLabel` |
| `URL-INV-CE-002` | `CLOSED` | ASCII form feed is whitespace in class tokens and visible labels | same retained battery and packet; finite class/paragraph inputs | normalize all five ASCII whitespace characters consistently |
| `URL-INV-CE-003` | `CLOSED` | `xmp` content is raw text, including literal IDL generics and apparent tags | same retained battery and packet; finite raw-generic and fake-definition inputs | tokenize raw `xmp` content without treating its interior as source markup |
| `URL-INV-CE-004` | `CLOSED` | IDL headers are ordinary named interfaces; missing names and unsupported mixins are rejected | same retained battery and packet; finite malformed/unsupported header inputs | recognize the contracted header shape rather than an `interface ` prefix alone |
| `URL-INV-CE-005` | `CLOSED` | Paragraph lexical spans stop at ordinary block tags such as section and form | same retained battery and packet; finite paragraph-boundary inputs | use the explicit block-boundary vocabulary in the interface record |
| `INFRA-TEXT-CE-001` | `CLOSED` | `JsString.splitOnCommas (ofLiteral " a , ,b,") = [ofLiteral "a", ofLiteral "", ofLiteral "b", ofLiteral ""]`, the source example at base `c1c7caa` | `WhatwgTest/Infra/Counterexamples/CommaSplit.lean`: `ce001_correct_result`, `ce001_original_result_false`, `ce001_empty_input`; packet, passing narrow/full builds and standard-base axiom receipts in `test/counterexamples/infra/COMMA-SPLIT.md`; full ordered token-list observation, finite kernel probes with no host assumptions | correct the unfrozen source example to three tokens; retain its original proposition under negation; preserve the algorithm and the distinct `strictlySplit` behavior; closed after coordinator compilation and the common axiom audit |
| `WS-SHA-CE-001` | `MOVED` | `Sha256.Spec.H0` is FIPS 180-4 §5.3.3 and not §5.3.2 | moved to lean4-hash `0168306` (`test/counterexamples/sha256/` there, same ID) with the SHA-256 lane; was: `WhatwgTest/Streams/Counterexamples/Sha/Mutants.lean`, `ce001_sha224IV` with its control `ce001_control`; `Sha256.Bridge.sha256_ne_sha224_iv` on the constants | none: the shipped `H0` is §5.3.3, and the witness pins that the choice is load-bearing |
| `WS-SHA-CE-002` | `MOVED` | `Sha256.Impl.padBytes` appends the 64-bit big-endian length of FIPS 180-4 §5.1.1 | moved to lean4-hash `0168306` (`test/counterexamples/sha256/` there, same ID) with the SHA-256 lane; was: same file, `ce002_noLengthField` on W2, with `ce002_padBytes_eq_on_empty` proving why W1 cannot discriminate | none to the implementation; the contract's claim that W1 catches this mutant is corrected in `test/counterexamples/sha/ATTACKS.md` |
| `WS-SHA-CE-003` | `MOVED` | `Sha256.Impl.wordOfBytes` reads four bytes big-endian per FIPS 180-4 §3.1 | moved to lean4-hash `0168306` (`test/counterexamples/sha256/` there, same ID) with the SHA-256 lane; was: same file, `ce003_littleEndianWords` on W2 | none: the shipped reading is big-endian |
| `WS-SHA-CE-004` | `MOVED` | `Sha256.Spec.bitsOfByte` is most-significant-bit-first per FIPS 180-4 §3.1, not FIPS 202 Appendix B.1's least-significant-first | moved to lean4-hash `0168306` (`test/counterexamples/sha256/` there, same ID) with the SHA-256 lane; was: same file, `ce004_lsbFirstBitOrder` on W1, with `ce004_padMarker` identifying the mutant's `0x01` padding byte | none: `Sha256.Spec` reproduces rather than imports foldlab's `formal/fips202` conversion, precisely because the conventions differ |
| `WS-SHA-CE-005` | `MOVED` | `Sha256.Impl.sha224` keeps the **left-most** 28 bytes of the untruncated output, per FIPS 180-4 §6.3 exception 2 | moved to lean4-hash `0168306` (`test/counterexamples/sha256/` there, same ID) with the SHA-256 lane; was: same file, `ce005_rightmostTruncation` on the SHA-224 `Len = 0` vector, with its control `ce005_control` and the statement of the shipped truncation `ce005_truncation_is_leftmost` | none: the shipped truncation is the left-most, and the witness pins that the byte range is load-bearing |
| `WS-DATA-CE-001` | `CLOSED` | `[[queueTotalSize]]` equals the sum of the sizes in `[[queue]]`, for the specification's own carrier | `WhatwgTest/Streams/Counterexamples/Data/Queue.lean`, `ce001_wf_broken` with `ce001_exact_total`, `ce001_rounded_total`, `ce001_gap`, `ce001_carriers_disagree`; the M1 half is `ce001_desired_size_exact` and `ce001_desired_size_rounded`; the step-6 half is `ce001_clamp_reachable_rounded` and `ce001_clamp_unreachable_exact` | rule `P3-R1`; state every invariant law under a `SizeClass.Exact` hypothesis, so a rounding instance loses the law instead of falsifying it. Closes when the ruled instance lands and `dequeueValue_wf` is proved against it |
| `WS-DATA-CE-002` | `CLOSED` | the fold direction of `sizeSum` is a matter of taste | same file, `ce002_folds_differ_rounded` with `ce002_left_fold_rounded`, `ce002_right_fold_rounded`, and the agreeing exact case `ce002_folds_agree_exact` | fold left, from `zero`, in the order `EnqueueValueWithSize` accumulates |
| `WS-DATA-CE-003` | `CLOSED` | a negative size may be appended and subtracted back later | same file, `ce003_mutant_accepts` with `ce003_reference_refuses`, `ce003_mutant_total_negative`, `ce003_wpt_minus_one_refused` | refuse before the append, with a `RangeError`, per `op.enqueue-value-with-size` step 2 |
| `WS-DATA-CE-004` | `CLOSED` | `DequeueValue` may return a default on an empty queue, since the emptiness condition is an assertion | same file, `ce004_mutant_answers` with `ce004_reference_refuses` and `ce004_agree_when_nonempty`, which proves no non-empty case discriminates the two | `Option` result, with `dequeueValue_isNone_iff` pinning refusal to exactly the empty queue |
| `WS-DATA-CE-005` | `CLOSED` | `PeekQueueValue` may thread the container through its result | same file, `ce005_mutant_mutates` and `ce005_mutant_not_idempotent`, with `ce005_mutant_same_value` proving a value-only comparison is blind to it | freeze the result type as `Option α` with no `SizeClass` argument, so the mutation is not expressible |
| `WS-DATA-CE-006` | `CLOSED` | `ResetQueue` may preserve a running total it has been carrying | same file, `ce006_mutant_keeps` with `ce006_reference_zeroes`, `ce006_entries_agree`, `ce006_reset_restores_invariant` | write `zero` outright; `resetQueue_wf` is the one invariant law with no carrier hypothesis |
| `WS-DATA-CE-007` | `CLOSED` | the `NaN`, `+∞` and negative refusal laws are satisfiable on a carrier that has no `NaN` and no infinities | same file, `ce007_nan_refusal_is_vacuous`, with `ce007_wpt_sizes_all_refused` over the pinned WPT list `[NaN, -Infinity, Infinity, -1]` | make `nan`, `posInfinity` and `negInfinity` fields of `SizeClass` and classify each in `SizeClass.Classified`, so an instance must exhibit them |
| `WS-DATA-CE-008` | `CLOSED` | one refusal predicate serves both `EnqueueValueWithSize` and `ExtractHighWaterMark` | same file, `ce008_mutant_refuses_pos_inf` with `ce008_reference_allows_pos_inf`, `ce008_rules_agree_off_pos_inf`, `ce008_wpt_hwms_all_refused` | two predicates; `+∞` refused as a size and accepted as a high water mark, frozen together in `extractHighWaterMark_disagrees_with_enqueue_on_posInfinity` |
| `WS-DATA-CE-009` | `CLOSED` | `ExtractHighWaterMark` normalizes its input, as its anchor id `validate-and-normalize-high-water-mark` suggests | same file, `ce009_mutant_changes_pos_inf` with `ce009_reference_is_the_identity_on_accepted` and `ce009_mutant_is_still_idempotent`, which proves an idempotence law does not catch the mutant | freeze `extractHighWaterMark_id_on_accepted`; there is no normalization step and no such algorithm at this pin |
| `WS-DATA-CE-010` | `CLOSED` | the queue may be a stack | same file, `ce010_mutant_is_lifo` with `ce010_reference_is_fifo`, `ce010_totals_agree`, `ce010_lengths_agree`, `ce010_peek_dequeue_agree_reference` | append on enqueue, remove index `0` on dequeue; totals, lengths and multisets do not separate the two |

| `WS-READ-CE-001` | `CLOSED` | size may be called before direct delivery | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce001_pending_skips_size`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `beginEnqueue_pending / pending_read_skips_size` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-002` | `CLOSED` | sized enqueue may prepend a chunk | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce002_queue_is_not_stack`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `finishEnqueue_value / fifo_close_m1_m2` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-003` | `CLOSED` | close may immediately hide a queued chunk | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce003_close_must_drain`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `close_deferred / fifo_close_m1_m2` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-004` | `CLOSED` | last chunk may settle before the closed promise | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce004_closed_before_last_chunk`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `read_queued_eq / fifo_close_m1_m2` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-005` | `CLOSED` | HWM zero rules out pull demand even with pending reads | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce005_zero_hwm_pending_read`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `shouldCallPull_iff` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-006` | `CLOSED` | demand while pulling may invoke a concurrent pull | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce006_pull_again_coalesces`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `callPullIfNeeded_busy` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-007` | `CLOSED` | pullAgain permits an unconditional invocation after fulfillment | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce007_fulfillment_rechecks_demand`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `reactPull_fulfilled / callPullIfNeeded_idle` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-008` | `CLOSED` | nested synchronous size invocations resume FIFO | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce008_nested_size_is_lifo`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `resumeSize_size / reentrant_enqueue_lifo` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-009` | `CLOSED` | CanCloseOrEnqueue must be rechecked after the size callback | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce009_closed_can_retain_chunk`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `finishEnqueue_value / reentrant_close_keeps_chunk / reentrant_error_keeps_chunk` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-010` | `CLOSED` | a read created by size receives the already-admitted outer chunk directly | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce010_read_during_size`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `reentrant_read_does_not_recheck` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-011` | `CLOSED` | a later throw or invalid size overwrites a reentrant stored error | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce011_first_error_survives`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `error_terminal / reentrant_error_then_throw / reentrant_error_then_invalid` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-012` | `CLOSED` | a foreign answer may execute immediately or a pull job may run inside a size frame | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce012_answers_are_queued`; finite independent witness, command and masks in `test/counterexamples/readable/ATTACKS.md` | proved `acceptPullAnswer_waiting / runPullJob_suspended / runPullJob_cons` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-013` | `CLOSED` | queued read settles before its synchronous pull callback can error the stream | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce013_pull_error_precedes_chunk`; also `harness/readable/reentrancy.mjs`, finite Node v22.23.2 host observation only; masks/command in `test/counterexamples/readable/ATTACKS.md` | proved `read_queued_eq / pull_reentrant_error_before_chunk` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-014` | `CLOSED` | the post-read queue always equals the immediate dequeue result despite synchronous pull | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce014_pull_can_change_queue`; also `harness/readable/reentrancy.mjs`, finite Node v22.23.2 host observation only; masks/command in `test/counterexamples/readable/ATTACKS.md` | proved `pull_reentrant_enqueue_changes_queue` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-015` | `CLOSED` | an outer queued read must settle before a nested later read in source pull | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce015_nested_read_overtakes`; also `harness/readable/reentrancy.mjs`, finite Node v22.23.2 host observation only; masks/command in `test/counterexamples/readable/ATTACKS.md` | proved `pull_reentrant_read_overtakes` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-016` | `CLOSED` | all model-generated RangeErrors may share one parameterless tag | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce016_generated_errors_are_distinct`; also `harness/readable/reentrancy.mjs`, finite Node v22.23.2 host observation only; masks/command in `test/counterexamples/readable/ATTACKS.md` | proved `Exception.ofRangeError_eq / Exception.rangeError_eq_iff / nested_invalid_sizes_fresh_errors` in P4a; local repair verified 2026-09-05 |
| `WS-READ-CE-017` | `CLOSED` | M2 may erase desiredSize query observations and require external state to recover M1 | `WhatwgTest/Streams/Counterexamples/Readable/Default.lean`, `ce017_query_is_visible`; finite witness; masks/command in `test/counterexamples/readable/ATTACKS.md` | proved `queryDesiredSize_m2 / observeM2_toM1` in P4a; local repair verified 2026-09-05 |

| `WS-WRITE-CE-001` | `CLOSED` | Size callback closes before admission. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce001_reentrant_size_close`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_afterSize_closing` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-002` | `CLOSED` | Abrupt size result yields an already stored error. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce002_thrown_size_existing_error`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_afterSize_errored` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-003` | `CLOSED` | Closing precedes erroring after size. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce003_closing_before_erroring`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_afterSize_closing` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-004` | `CLOSED` | Replacing ready retains older promise cells. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce004_ready_identity`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `updateBackpressure_true` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-005` | `CLOSED` | Close fulfills ready while backpressure remains true. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce005_close_keeps_backpressure`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_beginClose_admitted` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-006` | `CLOSED` | In-flight write keeps its chunk and total size. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce006_inflight_queue_retained`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_advance_write` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-007` | `CLOSED` | Current write fulfills before ready on drainage. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce007_write_before_ready`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_write_fulfilled` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-008` | `CLOSED` | Sink close invocation precedes algorithm clearing. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce008_close_clear_after_return`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `invokeSink_eq; attachSink_eq` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-009` | `CLOSED` | Pending abort removal precedes invocation; clearing follows return. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce009_abort_entry_slots`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_finishErroring; invokeSink_eq; attachSink_eq` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-010` | `CLOSED` | In-flight rejection retains the earlier stored reason. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce010_first_stored_error`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_write_rejected; tick_dealRejection` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-011` | `CLOSED` | Close success settles close, abort, closed; prior ready rejection remains. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce011_close_wins_erroring`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_close_fulfilled` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-012` | `CLOSED` | Post-signal terminal recheck precedes alias reuse. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce012_signal_terminal_recheck`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_afterSignal_terminal` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-013` | `CLOSED` | Signal is one-shot and live pending abort retains identity. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce013_nested_abort_identity`; finite fixture; coordinator host-only probe `harness/writable/reentrancy.mjs`; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_beginAbort_signaled; tick_afterSignal_existing` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-014` | `CLOSED` | Close sentinel has zero size. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce014_zero_size_close`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `tick_beginClose_admitted` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-015` | `CLOSED` | Invoking/queued operations reject settlement answers. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce015_answer_admission`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `acceptAnswer_other` in P5a; local production repair verified 2026-09-05 |
| `WS-WRITE-CE-016` | `CLOSED` | Reaction jobs wait behind live callback markers. | `WhatwgTest/Streams/Counterexamples/Writable/Default.lean`, `ce016_jobs_wait_for_callback`; finite fixture; scope/command in `test/counterexamples/writable/ATTACKS.md` | proved `externalFrontier_eq; tick_foreign_marker` in P5a; local production repair verified 2026-09-05 |

| `WS-WRITE-CE-017` | `CLOSED` | Unparenthesized equality before Boolean OR accepts an in-flight predicate that ignores close. | `WhatwgTest/Streams/Counterexamples/Writable/InFlightPrecedence.lean`, `weakLaw_parse` and `ce017_ignored_close_passes_weak`; Init-only, axiom-free parse/finite witness; receipt in `test/counterexamples/writable/ATTACKS.md` | proved `hasInFlight_exact` with parenthesized whole RHS; earlier weaker law retained unchanged; correct production definition and exact receipt verified 2026-09-05 |

Every row's evidence command is `lake build WhatwgTest`, which
elaborated the witnesses here until the SHA-256 lane moved to lean4-hash at
step 6 of `docs/HASH-PACKAGE-PLAN.md`; the rows, their kernel-checked
witnesses, and the attack shapes now live in that repository under the same
IDs, and `test/counterexamples/sha/ATTACKS.md` here is a pointer.

The `URL-CEN-CE-001` row closes a finite source-tooling regression. The
separate breaker observed both assertions fail before the repair. The
original census battery and both retained refusal probes pass unchanged,
and the default build checks 130 modules and 6896 declarations. The actual
`Gates.UrlCensus.assign` axiom receipt is
`[propext, Classical.choice, Quot.sound]`; there is no general theorem or
URL execution claim in these probes. All repository gates pass and the
independent reviewer confirms the whitespace repair. The source census and
coverage graph remain open.

The `URL-INV-CE-*` rows are finite source-tooling regressions frozen by a
separate breaker in `4e4e880`, `6db5ec2`, and `a60add5`. All ten assertions
failed against the initial scanner, then passed after implementation repairs.
`lake build WhatwgTest.Url.Counterexamples.Inventory` and the default
`lake build` pass, with the retained module imported by `WhatwgTest.lean`.
The common axiom gate checks 127 modules and 6774 declarations. The production
`scan` receipt is `[propext, Classical.choice, Quot.sound]`; the assertions
are executable `#guard` probes, not general theorems. All repository gates
and the URL projection drift gate pass. These rows close lexical regressions
only; the census graph in `docs/URL-CENSUS-DAG.md` remains open.

The `WS-DATA-*` rows were minted by the P3 queue-with-sizes breaker, 2026-09-02,
and are the first rows outside the `SHA` area. Their evidence command is
`lake build WhatwgTest.Streams.Counterexamples.Data.Queue`, which elaborates the
witnesses; each is closed by `decide`, so it is checked by the Lean kernel with
no compiler in the trust path, and every receipt is `none` or `propext`. The
attack shapes are described in `test/counterexamples/data/ATTACKS.md` and the
frozen surface they force is `test/contracts/queue-with-sizes.contract.md`.

The witnesses are breaker models in a `Breaker` namespace, not the production
declarations: they prove the attacks rather than the laws. The ten `WS-DATA-*`
rows closed 2026-09-02 when the P3 builder landed the frozen surface with all
99 theorems proved and the coordinator landed the ruled `P3-R1` instance
`Whatwg.Streams.Data.DyadicSize`; each row's forced repair is now a theorem of
`Whatwg.Streams.Data` with its receipt in `WhatwgTest/Streams/Data/QueueAxiomReport.lean`.

The seventeen `WS-READ-*` witnesses remain frozen at `f4394d8`. Their
production repairs are the named `Whatwg.Streams.Readable` theorems in
`Whatwg/Streams/Readable/Laws.lean` and `Reentrancy.lean`, plus the shared
exception adapter in `Whatwg/Streams/Boundary/Exception.lean`. The exact
signatures and all 94 receipts are checked by the readable contract and
axiom modules. `docs/READABLE-DAG.md` owns the landing commands, local
M1/M2 scope, independent review, and remaining global obligations.

## Transform breadth witnesses

| ID | Status | Attacked claim | Retained evidence | Required repair |
| --- | --- | --- | --- | --- |
| `WS-TRANS-CE-001` | `CLOSED` | replacing the backpressure slot may overwrite its old promise identity | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce001_old_resolved_new_pending`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.setBackpressure_change`, `settle_pending`, `freshInternal_old`, and `freshInternal_new`; exact old-cell resolution and fresh pending cell; local repair verified 2026-09-05 |
| `WS-TRANS-CE-002` | `CLOSED` | SourcePull may return the old resolved backpressure promise | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce002_source_pull_returns_new`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.sourcePull_realizes`; actual P4 pull-frame return/subscription uses the newly installed cell; local repair verified 2026-09-05 |
| `WS-TRANS-CE-003` | `CLOSED` | a blocked write subscription may follow later backpressure-slot replacements | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce003_write_captures_old`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.sinkWrite_blocked`, `subscribe_pending`, and `settle_pending`; registration captures the old identity; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |
| `WS-TRANS-CE-004` | `CLOSED` | resolving backpressure may immediately fulfill the pending P5 sink result | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce004_unblock_does_not_fulfill_sink`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.sinkWrite_blocked`, `notify_reaction`, `react_write_writable`, and `react_adopt`; unblock queues a continuation and its distinct result settles later; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |
| `WS-TRANS-CE-005` | `CLOSED` | writable state may be checked only when the write first waits | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce005_reaction_rechecks_erroring`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.react_write_erroring`; later reaction reads current P5 state and rejects with its stored reason; local repair verified 2026-09-05 |
| `WS-TRANS-CE-006` | `CLOSED` | a foreign transform answer may be accepted before callback return | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce006_return_precedes_answer`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.answerTransform_unmatched`, `answerTransform_waiting`, and `returnTransform_frame`; later answers require the retained returned-pending request; local repair verified 2026-09-05 |
| `WS-TRANS-CE-007` | `CLOSED` | reaction attachment may precede the synchronous transform return | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce007_return_precedes_attachment`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.returnTransform_frame`; the return event precedes registration/notification in the local adapter; local repair verified 2026-09-05 |
| `WS-TRANS-CE-008` | `CLOSED` | queued promise reactions may run inside an active transformer callback | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce008_no_job_inside_callback`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.tick_await_suspended` and `tick_await_nested_writable`; a live transformer frame blocks the coupled job queue; local repair verified 2026-09-05 |
| `WS-TRANS-CE-009` | `CLOSED` | readable backpressure may ignore pending reads at high-water mark zero | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce009_zero_hwm_pending_read`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.enqueue_same_pressure`, `enqueue_raises_pressure`, and `coupled_write_read_enqueue`; canonical P4 demand determines pressure after delivery; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |
| `WS-TRANS-CE-010` | `CLOSED` | termination may error the readable side or drop queued output | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce010_termination_drains`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.terminate_eq` with canonical `Readable.close_deferred` and `Readable.read_queued_eq`; queued output remains available while writable enters erroring; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |
| `WS-TRANS-CE-011` | `CLOSED` | one transform invocation must emit exactly one output chunk | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce011_multiple_outputs`; finite independent protocol projection, exact tape/scope in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.enqueue_same_pressure`, `enqueue_raises_pressure`, and `returnTransform_frame`; no per-invocation output-count restriction, canonical P4 queue/read laws retain FIFO; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |
| `WS-TRANS-CE-012` | `CLOSED` | backpressure change may allocate the new cell before queuing the old subscriber | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce012_resolution_queues_before_allocation`; finite protocol trace in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.setBackpressure_change` and `settle_pending`; old-cell settlement/notification precedes allocation; local repair verified 2026-09-05 |
| `WS-TRANS-CE-013` | `CLOSED` | clearing installed transform algorithms invalidates an already active callback return | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce013_return_survives_clear`; finite protocol trace in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.returnTransform_frame` and `coupled_error_during_transform`; return matches the retained frame independently of installed algorithms; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |
| `WS-TRANS-CE-014` | `CLOSED` | positive readable capacity may leave initial backpressure unresolved until an unnecessary read | `WhatwgTest/Streams/Counterexamples/Transform/Backpressure.lean`, `ce014_start_services_positive_capacity`; finite start/write protocol projection in `test/counterexamples/transform/ATTACKS.md` | proved `Transform.initial_eq`, `tick_native_pull`, and `coupled_positive_readable_capacity`; successful-start native pull precedes external admission; local repair verified 2026-09-05; finite Node projection in `harness/transform/backpressure.mjs` |

The fourteen `WS-TRANS-*` finite witnesses passed direct kernel verification on
2026-09-05. They do not execute the production P4/P5 state machines or establish
a host/DB-04 relation. Their command and axiom receipts are in the attack file.
The production local repairs above pass the frozen battery under
`TRANSFORM-PG-BACKPRESSURE`; full reachability, global scheduling, host and
DB-04 relations remain open. Names without a repeated namespace in a row
inherit that row's `Transform` namespace. Retained witnesses are unchanged.

## Piping breadth witnesses

P7a rows, 2026-09-05. All sixteen finite witnesses passed direct Lean
verification; freeze is recorded in the owning contract. The named local
production repairs now pass the unchanged P7a battery and R-11 receipts.
These fixtures are not host or whole-pipe evidence.

| ID | Status | Attacked claim | Retained evidence | Required repair |
| --- | --- | --- | --- | --- |
| `WS-PIPE-CE-001` | `CLOSED` | Immediate abort skips a pending write. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce001_pending_write_blocks`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `allWrittenSettled_iff`, `tick_eq`, `lifecycle_two_writes_abort_rejection`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-002` | `CLOSED` | A rejected write is treated as unfinished. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce002_rejected_write_settled`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `writesSettled_iff`, `allWrittenSettled_iff`, `lifecycle_write_rejection_retains_source`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-003` | `CLOSED` | Waiting for only an old final-write identity loses a newer write. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce003_stale_wait_target`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `allWrittenSettled_eq`, `recordAllowed_iff`, `lifecycle_two_writes_abort_rejection`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-004` | `CLOSED` | Equal chunk values collapse distinct read obligations. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce004_duplicate_chunk_ids`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `readInventory_iff`, `markWrite_eq`, `recordAllowed_iff`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-005` | `CLOSED` | The source queue is used to reconstruct already-read chunks. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce005_queue_drops_read_history`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `deliveredReads_eq`, `readInventory_iff`, `reaches_no_new_read`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-006` | `CLOSED` | Later shutdown requests replace the first reason. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce006_first_shutdown_wins`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `enterForwardShutdown_first_wins`, `reaches_selection_stable`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-007` | `CLOSED` | preventAbort bypasses the required write drain. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce007_prevent_abort_still_waits`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `tick_eq`, `recordAllowed_iff`, `lifecycle_write_rejection_retains_source`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-008` | `CLOSED` | A write rejection replaces the selected source error. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce008_write_error_no_override`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `recordAllowed_iff`, `preventAbort_write_rejection_no_override`, `lifecycle_write_rejection_retains_source`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-009` | `CLOSED` | A failed shutdown action keeps the original source error. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce009_action_error_overrides`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `recordAllowed_iff`, `tick_eq`, `lifecycle_two_writes_abort_rejection`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-010` | `CLOSED` | The destination guard is reevaluated after shutdown entry. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce010_capture_drain_gate`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `enterForwardShutdown_eq`, `recordAllowed_iff`, `tick_eq`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-011` | `CLOSED` | Intrinsic abort is replaced by a sink callback or by the stored error. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce011_terminal_abort_fulfills`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `invokeAbort_eq`, `recordAllowed_iff`, `lifecycle_write_rejection_retains_source`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-012` | `CLOSED` | Queued reactions run inside a live synchronous callback. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce012_callback_blocks_jobs`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `stepWritable_eq`, `externalFrontier_eq`, `tick_eq`, `imported Writable.tick`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-013` | `CLOSED` | Destination error is selected ahead of simultaneous source error. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce013_forward_error_priority`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `enterForwardShutdown_eq`, `tick_eq`, `recordAllowed_iff`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-014` | `CLOSED` | Returning a pending action promise counts as action completion. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce014_pending_action_is_live`; exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `captureAbort_eq`, `tick_eq`, `ready_frontier`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-015` | `CLOSED` | Destination erroring after the drain gate was latched drops an owed rejected write result. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce015_erroring_owed_write_keeps_wait`; two actual P5 stages at a prescribed snapshot, exact finite scope in `test/counterexamples/piping/ATTACKS.md` | `recordAllowed_iff`, `allWrittenSettled_iff`, `tick_eq`, `forwardShutdown_realizes`; canonical `Writable.tick`; proved frozen obligations; local repair verified 2026-09-05 |
| `WS-PIPE-CE-016` | `CLOSED` | A new read after shutdown is invisible because an errored source delivers no chunk. | `WhatwgTest/Streams/Counterexamples/Piping/Shutdown.lean`, `ce016_new_read_changes_snapshot`; actual canonical P4 finite read with changed nextRead and new rejected request | `recordAllowed_iff`, `reaches_no_new_read`, `forwardShutdown_realizes`; proved local repair verified 2026-09-05 |

The sixteen retained witnesses are unchanged. Each `CLOSED` status concerns
only its named local repair under `PIPING-PG-FORWARD-SHUTDOWN`, verified by
the frozen law/run battery and the root audit. Names without a repeated
namespace inherit `Whatwg.Streams.Piping`; canonical writable operations
retain their `Writable` owner. The actual-prefix run proofs and quantified
`forwardShutdown_realizes` add production evidence within their admitted
fragment; full lifecycle, progress, host and global M1/M2 relations remain
open. The graph's P7a landing receipt records commands and remaining edges.

## Promise-layer witnesses

Q3 rows, minted by the coordinator at integration. The attacks are frozen by
the Q3 packet breaker in `test/counterexamples/promise/ATTACKS.md` at
`ef960be`; the repairs landed on `promise/q3-builder` and were integrated at
`7377134`. `PROMISE-PG-FIRST` freezes **no** executable witness module: each
row below is discriminated by a quantified statement in one of the eight
fenced batteries under `WhatwgTest/Ecma262/`, `WhatwgTest/WebIdl/` and
`WhatwgTest/Streams/PromiseBridge.lean`, and the retained evidence is that
frozen ascription, which the builder may not change. The evidence command for
every row is `lake --wfail build WhatwgTest`, green with all 371 `#check`
ascriptions, 13 `example`s and 125 axiom receipts elaborating inside the R-11
ceiling. Adding finite witnesses is separate later evidence, not a substitute
for these statements. Masks and decisions are the contract's,
`test/contracts/promise-first-packet.contract.md`. No host was run and no WPT
case was replayed for these rows.

| ID | Status | Attacked statement | Witness / evidence | Forced repair |
| --- | --- | --- | --- | --- |
| `WS-PROM-CE-001` | `CLOSED` | `PullAnswer` and `PullReturn` collapse into `PromiseState Unit ε`, since they are isomorphic | `WhatwgTest/Streams/PromiseBridge.lean`, `Writable.sinkAnswer_eq`, `sinkReturn_eq`, `sinkReturnToShared_eq` and `sinkReturn_roundtrip` with the six preservation `example`s that close by `intros; rfl`; quantified statements, no finite witness; `test/counterexamples/promise/ATTACKS.md` row `WS-PROM-CE-001` | decision 6: three distinct carriers `Promise.State`, `Promise.Outcome` and `Promise.Returned`, with the isomorphisms as bridging lemmas; the six `rfl` receipts of `Whatwg/Streams/Writable/Laws.lean` are unchanged |
| `WS-PROM-CE-002` | `CLOSED` | a Streams function named in an `attribute [local simp]` set may `move`, becoming an `abbrev` over a general function | `WhatwgTest/Streams/PromiseBridge.lean`, the six equation-lemma `example`s re-deriving `Writable.lookupPromise_eq`, `Writable.markHandled_eq`, `Transform.lookupPromise_eq` and the three `subscriptionPromise_*` receipts by `rfl`; the four `attribute [local simp]` modules are byte-identical to `e819a9f`; ATTACKS.md `WS-PROM-CE-002` | decision 5 and inventory condition 3: every one of the twenty-one functions of the contract's §4.4 is `generalize` or `keep`, never `move`, so `simp [f]` keeps rewriting with `f`'s own equation lemmas |
| `WS-PROM-CE-003` | `CLOSED` | the general promise state has one type parameter, with the value fixed to `Unit` | `WhatwgTest/Ecma262/PromiseContract.lean`, `Promise.State : Type → Type → Type` with `Cell` and `Table` beside it; `WhatwgTest/Streams/PromiseBridge.lean`, `Readable.readTable` ascribed at value `Readable.ReadResult α`; ATTACKS.md `WS-PROM-CE-003` | `E-22` and R-P13: two parameters throughout, because `Readable.State.readPromises` is the second instance and its value is not `Unit` |
| `WS-PROM-CE-004` | `CLOSED` | `Outcome` takes two parameters, so `Outcome.fulfilled` carries a value | `WhatwgTest/Streams/PromiseBridge.lean`, `Readable.PullAnswer.fulfilled : ∀ {ε : Type}, PullAnswer ε`, with `PullAnswer.rejected` and `PullReturn.settled` beside it; ATTACKS.md `WS-PROM-CE-004` | decision 12: `Promise.Outcome : Type → Type` with a nullary `fulfilled`, so every `.fulfilled` in `Whatwg/Streams/**` resolves unchanged; the value-carrying settled outcome stays `Except reason value` and is not minted a second time |
| `WS-PROM-CE-005` | `CLOSED` | ES2026's assertion is reproduced literally, making `settle` partial or undefined on a settled promise | `WhatwgTest/Ecma262/PromiseLaws.lean`, `Table.settle_other` and `Table.settle_assert`, with `settle_pending` and `settle_handled`; ATTACKS.md `WS-PROM-CE-005` | decision 7: the total guarded `Table.settle`, the identity off a pending cell, with `op.fulfillpromise`'s assertion recovered as the side condition `settle_assert` |
| `WS-PROM-CE-006` | `CLOSED` | the table is a replacing map, so allocating overwrites an earlier cell | `WhatwgTest/Ecma262/PromiseLaws.lean`, `Table.fresh_old` and `Table.fresh_eq`; `WhatwgTest/Streams/PromiseBridge.lean`, `Writable.freshPromise_bridge` under `s.nextPromise ∉ s.handled`; ATTACKS.md `WS-PROM-CE-006` | `E-13`, `E-43`: an append-only entry list, which is the retention `Writable.updateBackpressure` depends on |
| `WS-PROM-CE-007` | `CLOSED` | an identity is reused, or the cursor advances by something other than one | `WhatwgTest/Ecma262/PromiseLaws.lean`, `Table.fresh_id` and `Table.fresh_next` over `fresh_eq`, above the retained P6 `Transform.initial` and P7 run witnesses that fix concrete identities; ATTACKS.md `WS-PROM-CE-007` | `E-14`, `E-23`, decision 3: `Nat` identities under a monotone cursor; `fresh` answers `t.next` and leaves `t.next + 1` |
| `WS-PROM-CE-008` | `CLOSED` | the job queue is a stack: prepend on enqueue, or consume the newest | `WhatwgTest/Ecma262/JobsLaws.lean`, `Jobs.Queue.enqueue_eq`, `Queue.dequeue_cons` and the general FIFO law `Queue.dequeue_fifo`, both mask M2; ATTACKS.md `WS-PROM-CE-008` | `E-45`: `Queue.enqueue` appends at the tail and `Queue.dequeue` removes the head, at all three Streams payloads |
| `WS-PROM-CE-009` | `CLOSED` | a job runs while a synchronous callback frame is live, or while another job is running | `WhatwgTest/Ecma262/JobsLaws.lean`, `Jobs.step_blocked` and `Jobs.RunCondition_iff`; `WhatwgTest/Streams/PromiseBridge.lean`, `Writable.tick_dequeue_bridge`, `Readable.runPullJob_dequeue_bridge` and `Transform.tick_dequeue_bridge`, whose hypotheses are the three Streams spellings of the empty execution-context stack; ATTACKS.md `WS-PROM-CE-009` | `G-08` for the *statement* of `requirement.jobs.1` (625769..626250) and `requirement.jobs.2` (626257..626350): `RunCondition` holds only at `Active.checkpoint` and `step` refuses elsewhere. The single global configuration that observes it is P8 and stays open |
| `WS-PROM-CE-010` | `CLOSED` | a job scheduled during a run overtakes one already scheduled | `WhatwgTest/Ecma262/JobsLaws.lean`, `Queue.dequeue_fifo` at nonempty `later`, `Jobs.run_fifo` and `Jobs.hostEnqueuePromiseJob_order`, all mask M2, with `FifoRequirement_iff` and `run_split`; ATTACKS.md `WS-PROM-CE-010` | the DB-05 specification/realizer pair R-P5 requires for `requirement.hostenqueuepromisejob.3` (634739..634841): `FifoRequirement` is the specification and `Jobs.run` the deterministic realizer |
| `WS-PROM-CE-011` | `CLOSED` | a reaction is triggered twice, or notified out of registration order | `WhatwgTest/Ecma262/PromiseLaws.lean`, `triggerReactions_order` (mask M2) and `triggerReactions_once`, with `triggerReactions_other_promise` and `_other_kind`; ATTACKS.md `WS-PROM-CE-011` | `E-33`, `G-01`: `op.triggerpromisereactions` (2700260..2701212) appends one reaction job per waiting reaction in list order and leaves that promise and kind with none waiting. New content, not extraction |
| `WS-PROM-CE-012` | `CLOSED` | one undifferentiated reaction list is presented as ES2026's two | `WhatwgTest/Ecma262/PromiseLaws.lean`, `Reactions.add_fulfill`, `add_reject`, `add_paired` and `registered_eq`; `WhatwgTest/Streams/PromiseBridge.lean`, `Transform.reactions_registered`; ATTACKS.md `WS-PROM-CE-012` | decision 8: two lists whose paired entries share one id in one order, with `Reactions.registered` the one-list view `Transform.State.subscriptions` instantiates; `Transform.Subscription` stays `keep` |
| `WS-PROM-CE-013` | `CLOSED` | `react` is total, answering for a promise identity that has no cell | `WhatwgTest/WebIdl/PromiseContract.lean`, `WebIdl.Promise.react_missing`; ATTACKS.md `WS-PROM-CE-013` | `E-31`: the Streams `Option` result is kept and the difference from `PerformPromiseThen` is recorded rather than smoothed |
| `WS-PROM-CE-014` | `CLOSED` | the handler runs immediately when `react` finds a settled promise, instead of queueing a reaction job | `WhatwgTest/WebIdl/PromiseContract.lean`, `react_fulfilled` and `react_rejected`, both mask M2, whose right-hand sides are `Jobs.Queue.enqueue`; ATTACKS.md `WS-PROM-CE-014` | `E-31`, `E-37`: `react` enqueues a `Jobs.ReactionJob` and moves the reaction to `ReactionPhase.queued`; nothing runs inside the call |
| `WS-PROM-CE-015` | `SEEDED` | `DecidableEq` is derived unconditionally on the general reaction or job record | `WhatwgTest/Ecma262/PromiseContract.lean` ascribes `inferInstance : Repr (Promise.Reaction Nat)` and ascribes no `DecidableEq`; `WhatwgTest/Ecma262/JobsContract.lean` ascribes both `Jobs.Queue Nat` instances conditionally on the payload. The discrimination is the *absence* of an ascription, so no frozen statement fails under the mutant and no finite witness exists; ATTACKS.md `WS-PROM-CE-015` | `E-29`, `E-50`: `Promise.Reaction` and `Transform.Job` derive `Repr` only, so `Transform.State` stays `Repr`-only in the same way. Seeded, not closed: a mechanical refusal needs a retained witness that a derived `DecidableEq` breaks, and none is frozen at this pin |
| `WS-PROM-CE-016` | `CLOSED` | Web IDL simple exceptions carry no allocation identity, as the pinned text has them | `WhatwgTest/WebIdl/ExceptionsContract.lean`, `Exceptions.Exception.simple_eq_iff`, `domException_eq_iff` and `identity_simple`; `WhatwgTest/Streams/PromiseBridge.lean`, `Boundary.Exception.toWebIdl_injective`; the retained P4a witness `nested_invalid_sizes_fresh_errors` of `Whatwg/Streams/Readable/Reentrancy.lean`, the repair of `WS-READ-CE-016` above; ATTACKS.md `WS-PROM-CE-016` | R-P14, `E-59`: every constructor of the general exception but `foreign` carries a `Nat`, and the embedding is injective, so two RangeErrors created by nested size callbacks stay distinguishable |
| `WS-PROM-CE-017` | `CLOSED` | `Boundary.Exception` is re-pointed as an `abbrev` of the general exception type | `WhatwgTest/Streams/PromiseBridge.lean`, the four `Boundary.Exception` preservation ascriptions with `rangeError_eq_iff` and the `ofRangeError_eq` `example`; the contract's builder note B1 reproduces the actual `lean.unknownIdentifier` diagnostics an `abbrev` produces for constructor names; ATTACKS.md `WS-PROM-CE-017` | `E-59` is `generalize`, not `move`: the Streams type keeps its three constructors and its `deriving DecidableEq, Repr`, so `.rangeError id` resolves unchanged in all 69 dependent theorems across eight files, and `toWebIdl`/`ofWebIdl` are the additive relation |
| `WS-PROM-CE-018` | `CLOSED` | the settled predicate is called `wait for all` | `WhatwgTest/WebIdl/PromiseContract.lean`, `AllSettled`, `allSettled` and `allSettled_iff` beside `waitForAll`, `waitForAll_pending`, `waitForAll_success` and `waitForAll_failure`; ATTACKS.md `WS-PROM-CE-018` | decision 10 with `E-55`, `E-56`: two separate names with two law sets, the operation carrying the ordered result and the short-circuit the predicate does not have; `op.waiting-for-all-promise` (354881..356058) and its microtask step stay out of the packet under `G-06` |
| `WS-PROM-CE-019` | `CLOSED` | `handled` moves into the cell and the relation to the Streams identity list is dropped | `WhatwgTest/Streams/PromiseBridge.lean`, `Writable.handled_bridge` and `Writable.markHandled_bridge` over `promiseTable_eq`; ATTACKS.md `WS-PROM-CE-019` | decision 9: the per-cell `handled : Bool` of `slot.PromiseIsHandled` (2746322..2746637), with `Writable.State.handled : List Nat` unchanged and the two related, so `HostPromiseRejectionTracker` has the field it will need |
| `WS-PROM-CE-020` | `CLOSED` | `[[AlreadyResolved]]` is ignored, so a resolving function settles its promise twice | `WhatwgTest/Ecma262/PromiseLaws.lean`, `ResolvingFunctions.callResolve_alreadyResolved` and `callReject_alreadyResolved`, with the two `_fresh` receipts that set the flag; ATTACKS.md `WS-PROM-CE-020` | `G-03`: `createResolvingFunctions` (2692679..2695411) carries the flag, and both calls are the identity on the table once it is set |
| `WS-PROM-CE-021` | `CLOSED` | a later rejection is reported instead of the first when `wait for all` short-circuits | `WhatwgTest/WebIdl/PromiseContract.lean`, `waitForAll_failure` (mask M2), whose hypothesis fixes every identity in `before` as fulfilled; ATTACKS.md `WS-PROM-CE-021` | `G-06`, partially: the reported reason is the first rejection in argument order, stated over `before ++ id :: after` |
| `WS-PROM-CE-022` | `CLOSED` | the `DOMException` name table has 33 entries, by adding `QuotaExceededError`, or repeats a name | `WhatwgTest/WebIdl/ExceptionsContract.lean`, `Exceptions.Name.all_length = 32`, `Name.all_nodup` and `Name.all_complete`, with the five-kind `Simple.all_*` trio; the contract's freeze recount of the 39 `type` rows of `generated/webidl-census.tsv`; ATTACKS.md `WS-PROM-CE-022` | R-P10: exactly the 32 base error names, one `type.` census row each, in ascending byte offset. At this pin `QuotaExceededError` is the derived interface `idl.quota-exceeded-error` (213527..213598) with no `type.` row, so it is not a `Name` constructor |
| `WS-PROM-CE-023` | `CLOSED` | `[[PromiseIsHandled]]` is set only on the branches that enqueue a job, so a promise still pending when `PerformPromiseThen` ran is left unmarked; this is the landed Q3 behaviour, frozen by the base `performPromiseThen_pending` | `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `Promise.Table.markHandled_marked` and `Promise.performPromiseThen_handled`, both mask M1; the amended `performPromiseThen_pending` (mask M1) of `WhatwgTest/Ecma262/PromiseLaws.lean`, the amended `WebIdl.Promise.react_pending` of `WhatwgTest/WebIdl/PromiseContract.lean`, and `WebIdl.Promise.react_handled` (mask M1) of `WhatwgTest/WebIdl/PromiseFidelityContract.lean`; ATTACKS.md `WS-PROM-CE-023` | finding F1 with addendum §4 rows 10 and 13..18, ratified by R-P22: `op.performpromisethen` (2740635..2743669) step 12 sits after the three-way branch, so every branch that returns at all marks the promise. `performPromiseThen_handled` quantifies over all three branches, `markHandled_marked` supplies the cell law of `slot.PromiseIsHandled` (2746322..2746637), and `react_handled` inherits step 12 through `op.dfn-perform-steps-once-promise-is-settled` (350075..352408) step 7. `G-02` keeps `HostPromiseRejectionTracker` out |
| `WS-PROM-CE-024` | `CLOSED` | a settled branch appends a reaction to both lists and advances only the triggered side's phase, leaving the opposite-side entry `waiting` on a promise that can never trigger it; this is the landed Q3 behaviour in both `performPromiseThen` and `WebIdl.Promise.react` | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `Promise.Reactions.mint`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `Reactions.mint_eq`, `mint_fulfill`, `mint_reject`, `mint_next`, `mint_reaction` and `mint_no_waiting`, all mask M1; the amended `performPromiseThen_fulfilled` and `performPromiseThen_rejected` (mask M2) of `WhatwgTest/Ecma262/PromiseLaws.lean` and the amended `react_fulfilled` and `react_rejected` (mask M2) of `WhatwgTest/WebIdl/PromiseContract.lean`; ATTACKS.md `WS-PROM-CE-024` | finding F2 with addendum §4 rows 3, 11, 12, 17 and 18: `op.performpromisethen` steps 10 and 11 build a job from one of the records of steps 7 and 8 and append nothing, so `Reactions.mint` returns the unappended record and the operation returns it. `mint_no_waiting` is the whole of F2 — minting leaves the waiting set of every promise and both lists exactly as it was — and the enqueued job is unchanged, which is why the four amended branch laws keep their M2 labels |
| `WS-PROM-CE-025` | `CLOSED` | settling clears only the list it triggered, leaving the other list's registrations `waiting`, and `triggerReactions_other_kind` is presented as the law that says so; this is the landed Q3 behaviour of `Table.settleAndTrigger` | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `Promise.Reactions.clear`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `Reactions.clear_eq`, `clear_waiting`, `clear_other_promise`, `clear_next` and `Table.settleAndTrigger_cleared`, all mask M1; the amended `Table.settleAndTrigger_pending` (mask M2) and the annotated `triggerReactions_other_kind` (mask M1) of `WhatwgTest/Ecma262/PromiseLaws.lean`; ATTACKS.md `WS-PROM-CE-025` | finding F3 with addendum §4 rows 9 and A1: `op.fulfillpromise` (2695419..2696230) steps 4 and 5 and `op.rejectpromise` (2699323..2700252) steps 4 and 5 each set **both** reaction lists to *undefined* after capturing the one they trigger, so the clearing belongs to the two callers. `triggerReactions_other_kind` keeps its statement and gains the dated annotation that it is a law about `op.triggerpromisereactions` (2700260..2701212) alone. Debt D19 records the departure that `clear` sets phases to `done` rather than removing entries, so the one-list view still sees a settled promise's registrations |
| `WS-PROM-CE-026` | `SEEDED` | `[[Capability]]` is omitted from the reaction record, so `performPromiseThen`'s capability argument is consumed by `Capability.promise` and stored nowhere and the `G-03` resolving functions settle nothing; this is the landed Q3 behaviour | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `Promise.Reaction.capability` with the `inferInstance : Repr (Reaction Nat)` re-check; the amended `Reaction.mk` at six arguments and `Reactions.add` at five of `WhatwgTest/Ecma262/PromiseContract.lean`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `Reactions.mint_capability`, `runReactionJob_resolve` and `runReactionJob_reject`, all mask M1; the amended `Transform.subscribe_reactions_bridge` (mask M1) of `WhatwgTest/Streams/PromiseBridge.lean`; ATTACKS.md `WS-PROM-CE-026`, whose reference to a statement `Reactions.add_capability` names nothing that exists and is superseded by R-P24 | finding F4 with addendum §4 rows 1, 2 and 19: `record.promisereaction-records` (2690445..2692671) lists `[[Capability]]` first at `field.promisereaction-records.Capability` (2691475..2691802), and `op.newpromisereactionjob` (2702885..2705591) reads it, so the field lands with `Reaction` still `Repr`-only under `E-50`. Seeded, not closed (R-P24): the capability is stored but never read — `runReactionJob` settles through whatever resolving functions the caller supplies, and no law ties them to the capability's promise. That law is debt D17 |
| `WS-PROM-CE-027` | `CLOSED` | the reaction job runs its handler: the handler is stored as a Lean function, or `runReactionJob` computes the handler's result | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `Promise.reactionHandlerResult`, `ResolvingFunctions.callSettle` and `runReactionJob`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `reactionHandlerResult_empty` and `reactionHandlerResult_handler` with `callSettle_ok`, `callSettle_error`, `runReactionJob_no_capability`, `runReactionJob_empty_handler`, `runReactionJob_resolve`, `runReactionJob_reject` and `runReactionJob_once`, all mask M1; ATTACKS.md `WS-PROM-CE-027` | finding F4 under DB-02: `field.promisereaction-records.Handler` (2692151..2692611) is a JobCallback Record or `~empty~`, a descriptor, so `Reaction.handler : Option body` stays first-order and the handler's completion arrives as `reactionHandlerResult`'s `decision` argument, which `reactionHandlerResult_handler` returns unexamined. `reactionHandlerResult_empty` is inner steps 4a and 4b of `op.newpromisereactionjob`, and `requirement.jobs.4` (626486..626589) is why the step is total. Debt D18 owes inner step 6a's assertion |
| `WS-PROM-CE-028` | `CLOSED` | `requirement.jobs.3` is claimed from `run`'s recursion alone, with `Active.job` produced by no operation and `run` never consulting the activation; this is the landed Q3 state, where the `semantics` edge names COMPLETE and nothing realizes it | `WhatwgTest/Ecma262/JobsCompleteContract.lean`, `Jobs.startJob`, `Jobs.completeJob` and `Jobs.RunToCompletion` with `startJob_checkpoint`, `startJob_queue`, `run_one_job_per_step` and `startJob_run_agree` (mask M2) and `startJob_blocked`, `startJob_active`, `completeJob_job`, `completeJob_other`, `RunToCompletion_iff`, `run_to_completion` and `run_to_completion_nonvacuous` (mask M1); ATTACKS.md `WS-PROM-CE-028` | finding F5 as R-P22 adopts it: the `Active`-threaded step, not discharge by typing. `requirement.jobs.3` (626357..626479) is the DB-05 specification `RunToCompletion`, `run_to_completion` its realizer and `run_to_completion_nonvacuous` the receipt that it bites, because the activation `startJob` produces is one in which `RunCondition` fails; `startJob_run_agree` keeps `Jobs.run_fifo`, `run_split`, `hostEnqueuePromiseJob_order` and both `tick_job_fifo` corollaries statement- and proof-identical. R-P24 qualifies the close: `startJob` and `completeJob` are reachable only inside `Jobs.lean`'s own laws and the global configuration that threads the activation is P8, the `G-08` remainder |
| `WS-PROM-CE-029` | `CLOSED` | every argument promise must be settled before a failure is reported, so a rejection while a sibling is still pending answers `pending`; this is the landed Q3 behaviour, since `waitForAll` guards on `allSettled` | `WhatwgTest/WebIdl/PromiseFidelityContract.lean`, `Promise.waitForAllTraced` with `waitForAllTraced_failure_pending` (mask M2), beside `waitForAllTraced_pending`, `waitForAllTraced_success` and `waitForAll_traced_success_agree` (mask M1); ATTACKS.md `WS-PROM-CE-029` | finding F6 as R-P22 adopts it: `op.wait-for-all` (353239..354879) sets `rejected` and performs `failureSteps` inside the rejection handler, which mentions neither `fullfilledCount` nor `total`, so `waitForAllTraced_failure_pending` fixes a sibling as pending and still requires `failure`. `waitForAll` keeps its signature and its eleven Streams call sites under `E-55` and `E-56`; `op.waiting-for-all-promise` (354881..356058) stays out under the `G-06` remainder. Debt D21 records that neither form performs the per-argument registrations the text performs |
| `WS-PROM-CE-030` | `CLOSED` | the first rejection in **argument** order is reported rather than the first to settle, and the result is labelled M2; this is the landed Q3 behaviour, since `waitForAll` uses `List.findSome?` over `ids` | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `Promise.SettlementTrace.firstRejection`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `firstRejection_eq` (mask M1) and `firstRejection_hit` (mask M2); `WhatwgTest/WebIdl/PromiseFidelityContract.lean`, `waitForAllTraced_failure` (mask M2); the relabelled `waitForAll_failure` and `waitForAll_success`, mask M2 → M1, of `WhatwgTest/WebIdl/PromiseContract.lean`; ATTACKS.md `WS-PROM-CE-030` | finding F6 with addendum §4 rows A2 and A3, ratified by R-P22: `waitForAllTraced_failure` and `firstRejection_hit` quantify over the settlement trace and fix the earlier *trace* entries, so the reported reason is the first rejection **to settle**. The two `waitForAll_*` statements are unchanged and frozen; under §7's rule argument order is neither a settlement order nor a queue order, so their M2 labels become M1 |
| `WS-PROM-CE-031` | `CLOSED` | the settlement order is carried in a second promise table, or in a field of `Table` that changes `Table.mk`'s arity | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `Promise.SettlementTrace` and `Table.settleTraced`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `Table.settleTraced_table` and `settleTraced_other` (mask M1) with `settleTraced_pending` (mask M2) and `Whatwg.Streams.Writable.settlementTrace_bridge` (mask M2); ATTACKS.md `WS-PROM-CE-031` | finding F6 under R-P12's no-shadow-table rule: `settleTraced_table` requires `(settleTraced t tr id r).1 = Table.settle t id r`, so the table is the same table and `Table.mk`'s arity is untouched, which is why every Q3 ascription over `Table` stands. The trace is shaped after `E-71`'s `Writable.settlementTrace`, whose content is unchanged, so the bridge needs no projection; `E-63`'s readable bridge is deferred with its stated reason. Debt D20 records that no coherence invariant relates the trace to the table and no ECMA-262 operation produces one |
| `WS-PROM-CE-032` | `CLOSED` | each resolving function carries its own `[[AlreadyResolved]]` flag, so calling `[[Resolve]]` leaves `[[Reject]]` armed | `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `ResolvingFunctions.callResolve_disables_reject` and `callReject_disables_resolve`, both mask M1, beside the `callSettle_ok` and `callSettle_error` selection laws; ATTACKS.md `WS-PROM-CE-032` | the anachronism minor, addendum §7.4: at this pin `op.createresolvingfunctions` (2692679..2695411) has no `[[AlreadyResolved]]` record at all — step 1 creates one shared Record `{ [[Value]]: toResolve }` that both closures capture, and each closure's step 3 empties it — so calling either function disables both. The field keeps the name `alreadyResolved` with the dated anachronism note, because renaming it would break four frozen ascriptions for a citation defect; only the citation changes |
| `WS-PROM-CE-033` | `SEEDED` | a promise is resolved with itself and fulfilled, because the general `value` cannot be compared to a promise identity | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, `ResolvingFunctions.callResolveSelf`; `WhatwgTest/Ecma262/PromiseFidelityLaws.lean`, `callResolveSelf_self` and `callResolveSelf_other`, both mask M1; ATTACKS.md `WS-PROM-CE-033` | the self-resolution minor: `op.createresolvingfunctions` resolve steps step 4 rejects with a newly created *TypeError* when `SameValue(resolution, promise)` is *true*, which `callResolveSelf_self` states, and `callResolveSelf_other` answers `none` off the branch rather than guessing, because the value universe is `G-04` and thenable adoption is `G-05`. Seeded, not closed (R-P24): the mutant lives in `callResolve`, whose statements are unchanged, and `callResolveSelf` has no caller |
| `WS-PROM-CE-034` | `CLOSED` | `newPromise`'s `Nat` is what `op.a-new-promise` returns | `WhatwgTest/WebIdl/PromiseFidelityContract.lean`, `Promise.newPromiseWithCapability` with `newPromiseWithCapability_eq` and `newPromise_capability_promise`, both mask M1; ATTACKS.md `WS-PROM-CE-034` | the capability minor: `op.a-new-promise` (347604..347946) step 2 returns `NewPromiseCapability(constructor)`, a PromiseCapability Record, and `newPromiseWithCapability` is that form. `newPromise_capability_promise` exhibits the landed identity as the capability's `[[Promise]]`, so `newPromise` keeps its signature and the eighteen `Whatwg/Streams/**` call sites that use the identity are untouched |
| `WS-PROM-CE-035` | `CLOSED` | the direct table settle is presented as `op.resolve` or `op.reject` | `WhatwgTest/WebIdl/PromiseFidelityContract.lean`, `Promise.resolveThrough` and `Promise.rejectThrough` with `resolveThrough_eq`, `rejectThrough_eq`, `resolveThrough_resolve` and `rejectThrough_reject`, all mask M1; ATTACKS.md `WS-PROM-CE-035` | the one-shot-marker minor: `op.resolve` (349216..349783) step 3 and `op.reject` (349785..350073) step 1 both `Call` the capability's `[[Resolve]]`/`[[Reject]]`, so they inherit the shared marker a direct settle does not have. `resolveThrough_resolve` and `rejectThrough_reject` state the exact hypothesis `f.alreadyResolved = false` under which the two agree, and `resolve` and `reject` keep their signatures and their 58 Streams invocations under `E-20` |
| `WS-PROM-CE-036` | `SEEDED` | `Reaction.handler : Option body` is justified by Web IDL `react` | `WhatwgTest/WebIdl/PromiseFidelityContract.lean`, `react_performPromiseThen` and `react_handled`, both mask M1, beside `uponFulfillment_eq` and `uponRejection_eq` of `WhatwgTest/WebIdl/PromiseContract.lean`, which the addendum leaves byte-identical; ATTACKS.md `WS-PROM-CE-036` | the §4.6 citation minor: `op.dfn-perform-steps-once-promise-is-settled` (350075..352408) steps 1 to 4 build **both** handlers unconditionally, so Web IDL `react` never registers an empty one; the `~empty~` handler comes from `op.performpromisethen` steps 3 and 5, the `IsCallable` test, and the justification is `field.promisereaction-records.Handler` (2692151..2692611). Seeded, not closed (R-P24): the repair is documentary — the citation is corrected and the `none` the two Web IDL laws freeze is recorded as a `G-11` modelling restriction — and no statement changes, so nothing mechanically rejects the mutant. Debt D22 records why F4 is unobservable through this surface |
| `WS-PROM-CE-037` | `SEEDED` | a theorem is labelled M2 because it mentions a list, or M1 because it is easy | the four relabelled ascriptions, each with its dated annotation and its statement unchanged: `Jobs.run_cons`, mask M2 confirmed, of `WhatwgTest/Ecma262/JobsLaws.lean`; `WebIdl.Promise.waitForAll_failure` and `waitForAll_success`, mask M2 → M1, of `WhatwgTest/WebIdl/PromiseContract.lean`; and `Streams.Writable.attachSink_pending_jobs`, mask M2 → M1, of `WhatwgTest/Streams/PromiseBridge.lean`; ATTACKS.md `WS-PROM-CE-037` | the mask minor, addendum §4.1 as R-P22 ratifies it: §7's *rule* is unchanged and only its enumeration is amended — `run_cons` makes the twelve thirteen, the two `waitForAll_*` leave it, and `attachSink_pending_jobs` was never in it — and acceptance condition 5's three docstrings in `Whatwg/Ecma262/Jobs.lean`, `Whatwg/WebIdl/Promise.lean` and `Whatwg/Streams/Writable/Laws.lean` are corrected to match. Seeded, not closed (R-P24): a mask label is a reading, and no frozen statement fails under a mislabelled one |
| `WS-PROM-CE-038` | `SEEDED` | an addendum ascription cites neither an inventory row nor a gap id, as `E-50` and `E-51` were cited in the contract, the graph and `ATTACKS.md` but in no battery | `WhatwgTest/Ecma262/PromiseFidelityContract.lean`, the `E-50` citation on `Promise.Reaction.capability` and on the `inferInstance : Repr (Reaction Nat)` re-check and the `E-51` citations on `reactionHandlerResult` and `runReactionJob`; `WhatwgTest/Ecma262/JobsCompleteContract.lean`, the `E-51` citation on `Jobs.startJob`; ATTACKS.md `WS-PROM-CE-038` | R-P12 with acceptance condition 1 of the base packet: every ascription of the four Q3b batteries names an inventory row with its reuse mode or a gap id, and `E-50` and `E-51` reach a battery for the first time. Seeded, not closed (R-P24): the row is mechanical only through the `inferInstance : Repr (Reaction Nat)` re-check, which goes red if the capability field forces `DecidableEq` on `Reaction`; a citation absent from a docstring refuses nothing |

Twenty-one of these rows close on a frozen statement the mutant makes fail.
`WS-PROM-CE-015` does not: the packet discriminates it by *not* ascribing a
`DecidableEq` instance, and an absent ascription refuses nothing mechanically,
so that row is `SEEDED` until a witness pins it. Names without a repeated
namespace inherit the owner of the module named in the same cell,
`Whatwg.Ecma262`, `Whatwg.WebIdl` or `Whatwg.Streams`. Every span is a byte
offset into the pinned `vendor/ecma262-0248456c/spec.html` or
`vendor/whatwg-webidl-a652053f/index.bs` of the contract's pin table; ends are
exclusive. These rows concern the packet's own statements only: `G-02`,
`G-04`, `G-05`, `G-10`, the `G-06`, `G-07`, `G-08`, `G-09` and `G-11`
remainders, and every group `G` inventory row `E-62`..`E-74`, stay open, and no
row here is a coverage, host or engine-equivalence claim.

That count is the twenty-two Q3 rows'. `WS-PROM-CE-023`..`WS-PROM-CE-038` are
the Q3b fidelity addendum's, one per finding `F1`..`F6` of ruling R-P20 and one
per minor it lists, frozen by the addendum breaker in
`test/counterexamples/promise/ATTACKS.md` at `38a806a` and answered by the
addendum `test/contracts/promise-first-packet-q3b.contract.md`, whose §4 rows
name each amended Q3 ascription. Nine of the sixteen attack the landed Q3
implementation *as built* rather than a hypothetical mutant, and each says so.
The evidence command for every one is `lake --wfail build WhatwgTest`, green at
the Q3b landing with all nine declared batteries elaborating, the addendum's 75
ascriptions and 57 receipts inside the R-11 ceiling and the base packet's 125
receipts still exactly 125 names. Statuses are R-P24's and are not the register
seat's to choose: eleven close on a frozen statement the mutant makes fail, and
five are `SEEDED` — `WS-PROM-CE-026`, because the capability is stored and never
read and the law tying `runReactionJob`'s resolving functions to it is debt D17;
`WS-PROM-CE-033`, because the mutant lives in the unchanged `callResolve` and
`callResolveSelf` has no caller; `WS-PROM-CE-036` and `WS-PROM-CE-037`, whose
repairs are documentary, a corrected citation and four corrected mask labels
over unchanged statements; and `WS-PROM-CE-038`, mechanical only through the
`inferInstance : Repr (Reaction Nat)` re-check. Every span is a byte offset into
the same two pinned sources; ends are exclusive. No host was run, no WPT case
was replayed, and both censuses stay all-`absent`, so no row here moves a
coverage state either.
